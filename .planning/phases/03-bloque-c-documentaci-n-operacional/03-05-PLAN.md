---
phase: 03-bloque-c-documentaci-n-operacional
plan: 05
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - CONTRIBUTING.md
  - CODE_OF_CONDUCT.md
autonomous: true
requirements: [DOC-03, DOC-04, DOC-09]
must_haves:
  truths:
    - "CONTRIBUTING.md covers the 6 required ejes + the DOC-09 ADR-in-same-PR rule + migrated dev tooling (codegraph/references/venv) + CI-vs-runtime divergence note"
    - "CODE_OF_CONDUCT.md is Contributor Covenant 2.1 with the enforcement contact filled in (no [INSERT CONTACT] placeholder)"
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "6-axis contributor guide + ADR rule + dev tooling"
      contains: "Conventional Commits"
    - path: "CODE_OF_CONDUCT.md"
      provides: "Contributor Covenant 2.1 with enforcement email"
      contains: "Contributor Covenant"
  key_links:
    - from: "CONTRIBUTING.md"
      to: "docs/adr/README.md"
      via: "DOC-09 ADR rule link"
      pattern: "docs/adr"
---

<objective>
Create CONTRIBUTING.md (the 6 required ejes + DOC-09 ADR rule + dev tooling migrated
from README + CI/runtime divergence note) and CODE_OF_CONDUCT.md (Contributor
Covenant 2.1 with the enforcement email filled in). Both in English (D-01).

Purpose: Cover DOC-03, DOC-04, and DOC-09. CONTRIBUTING.md is what the external dev
follows in the DOC-10 smoke test and what the OCA reviewer checks for contributor
process. It must cover all 6 ejes (setup, branch naming, Conventional Commits, code
review, testing ≥80%, pre-commit), the DOC-09 rule (architectural change = new ADR in
the same PR), and the codegraph/references/venv tooling moved out of README (D-02).

Wave 3: CONTRIBUTING links docs/adr/README.md (03-02, Wave 2). The release-process
section is a placeholder owned by Phase 4 REL-06 (Deferred).

Output: CONTRIBUTING.md (new), CODE_OF_CONDUCT.md (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-bloque-c-documentaci-n-operacional/03-CONTEXT.md
@.planning/phases/03-bloque-c-documentaci-n-operacional/03-RESEARCH.md
@.planning/phases/03-bloque-c-documentaci-n-operacional/03-PATTERNS.md
@SECURITY.md
@infra/docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CONTRIBUTING.md (6 ejes + DOC-09 + migrated tooling + CI/runtime note)</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - README.md (current codegraph/references/venv tooling blocks lines ~37-54 and ~69-80 — these MOVE here; in Wave 3 the README rewrite in 03-04 removes them)
    - infra/docker-compose.yml (dev setup command + test command)
    - .planning/phases/03-bloque-c-documentaci-n-operacional/03-PATTERNS.md (CONTRIBUTING.md section: dev setup, branch naming, Conventional Commits, ADR rule, testing patterns)
    - .planning/phases/03-bloque-c-documentaci-n-operacional/03-RESEARCH.md (D-03/D-07/D-09; Code Examples: CONTRIBUTING env divergence note; Claude's Discretion: document CI py3.10/PG12 vs local py3.11+/PG15+ A-01)
    - .planning/phases/03-bloque-c-documentaci-n-operacional/03-CONTEXT.md (D-09 ADR-same-PR rule; Deferred: release process is Phase 4 REL-06 placeholder only)
    - docs/adr/README.md (the ADR index the DOC-09 rule links to — created in plan 03-02)
  </read_first>
  <action>
    Create CONTRIBUTING.md in English (D-01) covering all 6 required ejes plus DOC-09:
    1. `## Development setup` — Docker 24+ / Compose v2, Python 3.11+ for tooling, git;
       first-time setup: clone → `docker compose -f infra/docker-compose.yml up -d` → create
       dev DB (l10n_py_dev, Country Paraguay, Demo NO) → `pip install pre-commit && pre-commit install`
       → optional `python scripts/build_index.py` for codegraph. Include the codegraph/references
       usage migrated from README (the bin/codegraph search/symbol/file examples).
    2. `## CI environment vs local runtime` — the A-01 divergence note: CI uses
       ghcr.io/oca/oca-ci/py3.10-odoo18.0 (Python 3.10, PostgreSQL 12); local uses odoo:18.0
       (Python 3.12) + postgres:16; both supported; if local-passes/CI-fails, check py3.10/3.12
       or PostgreSQL version behavior.
    3. `## Branch naming` — feature/* | fix/* | refactor/* | docs/* | chore/*; branch from main; <48h.
    4. `## Commit messages` — Conventional Commits with l10n_py examples (feat/fix/docs/test/chore).
    5. `## Code review` — PR required, 6 status checks green, conversation resolution.
    6. `## Testing` — minimum 80% line coverage for new logic in models/ and tools/;
       TransactionCase unit tests; the run command from infra/docker-compose.yml.
    7. `## Pre-commit hooks` — list the OCA hook set including oca-gen-addon-readme (D-07).
    8. `## Architectural changes` (DOC-09) — any change to module structure / data model /
       integration strategy requires a new ADR in docs/adr/ IN THE SAME PR; link
       docs/adr/README.md; ADRs merge as Proposed, maintainer flips to Accepted.
    9. `## Release process` — a short placeholder noting the semantic-release-vs-manual decision
       and steps are documented by Phase 4 (REL-06) — do NOT write the release process content
       (Deferred).
    Commit: `docs(contributing): 6-axis guide + ADR rule + dev tooling`.
  </action>
  <verify>
    <automated>test -f CONTRIBUTING.md && grep -q "Conventional Commits" CONTRIBUTING.md && grep -q "pre-commit" CONTRIBUTING.md && grep -Eq "80%|coverage" CONTRIBUTING.md && grep -q "docs/adr" CONTRIBUTING.md && grep -Eq "branch|feature/" CONTRIBUTING.md && grep -q "docker compose -f infra/docker-compose.yml" CONTRIBUTING.md</automated>
  </verify>
  <acceptance_criteria>
    - CONTRIBUTING.md exists
    - All 6 ejes present (grep matches): setup (`docker compose -f infra/docker-compose.yml`), branch naming (`feature/`), `Conventional Commits`, code review (`status check` or `review`), testing (`80%` or `coverage`), `pre-commit`
    - DOC-09 rule present: contains `docs/adr` and language requiring an ADR in the same PR for architectural changes
    - CI-vs-runtime divergence note present (grep `py3.10` and `postgres:16` / `3.12`)
    - codegraph/references tooling migrated in (grep `codegraph`)
    - Release process is a placeholder deferring to Phase 4 (no full release procedure written)
    - CONTRIBUTING is in English (D-01)
  </acceptance_criteria>
  <done>CONTRIBUTING.md covers the 6 ejes, the DOC-09 ADR rule, migrated dev tooling, and the CI/runtime divergence; release process is a Phase 4 placeholder.</done>
</task>

<task type="auto">
  <name>Task 2: Write CODE_OF_CONDUCT.md (Contributor Covenant 2.1, enforcement email filled)</name>
  <files>CODE_OF_CONDUCT.md</files>
  <read_first>
    - SECURITY.md (the enforcement-contact pattern — same maintainer email careagaezz@gmail.com)
    - .planning/phases/03-bloque-c-documentaci-n-operacional/03-RESEARCH.md (D-04; Pitfall 5: the single [INSERT CONTACT METHOD] placeholder must be replaced; Don't Hand-Roll: use CC 2.1 verbatim)
    - .planning/phases/03-bloque-c-documentaci-n-operacional/03-PATTERNS.md (CODE_OF_CONDUCT.md section: contact placeholder pattern + verification grep)
  </read_first>
  <action>
    Create CODE_OF_CONDUCT.md with the Contributor Covenant 2.1 text verbatim
    (contributor-covenant.org/version/2/1/code_of_conduct/), in English (D-01). Replace the
    single `[INSERT CONTACT METHOD]` placeholder with `careagaezz@gmail.com` (D-04 — same channel
    as SECURITY.md fallback). Do not hand-roll a custom CoC. Verify no placeholder remains.
    Commit: `docs(coc): add Contributor Covenant 2.1`.
  </action>
  <verify>
    <automated>test -f CODE_OF_CONDUCT.md && grep -q "Contributor Covenant" CODE_OF_CONDUCT.md && grep -q "careagaezz@gmail.com" CODE_OF_CONDUCT.md && ! grep -q "INSERT CONTACT" CODE_OF_CONDUCT.md</automated>
  </verify>
  <acceptance_criteria>
    - CODE_OF_CONDUCT.md exists and contains `Contributor Covenant` and `2.1`
    - `grep "INSERT CONTACT" CODE_OF_CONDUCT.md` returns no output (Pitfall 5 — placeholder replaced)
    - Contains `careagaezz@gmail.com` as the enforcement contact (D-04)
    - Text is the CC 2.1 standard (not a custom rewrite)
    - In English (D-01)
  </acceptance_criteria>
  <done>CODE_OF_CONDUCT.md is Contributor Covenant 2.1 with the enforcement email filled in and no remaining placeholder.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                                                 |
| ------------------------------------------ | --------------------------------------------------------------------------- |
| CODE_OF_CONDUCT enforcement email → public | Maintainer email is published in the CoC (intentional, same as SECURITY.md) |
| CONTRIBUTING dev commands → public         | Setup commands could leak infra detail if not generic                       |

## STRIDE Threat Register

| Threat ID | Category               | Component                         | Disposition | Mitigation Plan                                                                                                                                                      |
| --------- | ---------------------- | --------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-03-10   | Information Disclosure | CODE_OF_CONDUCT enforcement email | accept      | careagaezz@gmail.com is intentionally public per D-04 and already exposed in SECURITY.md — no new exposure                                                           |
| T-03-11   | Information Disclosure | CONTRIBUTING dev setup commands   | mitigate    | Commands use the public dev compose + default dev creds only; no production secrets referenced                                                                       |
| T-03-SC   | Tampering              | npm/pip/cargo installs            | accept      | No package installs in this plan; `pip install pre-commit` instruction is documentation pointing at an already-pinned dev tool, not an install executed by this plan |

</threat_model>

<verification>
- `grep -c "Conventional Commits\|pre-commit\|80%\|docs/adr" CONTRIBUTING.md` covers DOC-03 + DOC-09
- `grep "INSERT CONTACT" CODE_OF_CONDUCT.md || echo OK` returns OK (DOC-04, Pitfall 5)
- `pre-commit run --all-files` clean (prettier may reformat — re-stage)
</verification>

<success_criteria>

- CONTRIBUTING.md covers 6 ejes + DOC-09 ADR rule + migrated tooling (DOC-03, DOC-09)
- CODE_OF_CONDUCT.md is CC 2.1 with enforcement email, no placeholder (DOC-04)
  </success_criteria>

<output>
Create `.planning/phases/03-bloque-c-documentaci-n-operacional/03-05-SUMMARY.md` when done
</output>
