---
phase: 03-bloque-c-documentaci-n-operacional
plan: 06
type: execute
wave: 4
depends_on: ["03-04", "03-05"]
files_modified:
  - .planning/phases/03-bloque-c-documentaci-n-operacional/DOC-10-smoke-checklist.md
autonomous: false
requirements: [DOC-10]
must_haves:
  truths:
    - "A GitHub issue exists tracking the DOC-10 external-dev smoke test with a clone→setup→docker→install→tests checklist"
    - "The phase is 'implemented' on doc merge; DOC-10 is async UAT that does not block Phase 4"
  artifacts:
    - path: ".planning/phases/03-bloque-c-documentaci-n-operacional/DOC-10-smoke-checklist.md"
      provides: "The smoke-test checklist body used to open the GitHub issue"
      contains: "docker compose"
  key_links:
    - from: "DOC-10 issue"
      to: "CONTRIBUTING.md"
      via: "the external dev follows CONTRIBUTING.md only"
      pattern: "CONTRIBUTING"
---

<objective>
Create the DOC-10 smoke-test GitHub issue (D-16): an external dev with no repo
context follows CONTRIBUTING.md on their own machine, and the result is tracked via
a GitHub issue with a clone → setup → docker up → modules installed → tests run
checklist plus a friction-log section.

Purpose: Cover DOC-10. Per D-16, the phase is "implemented" once the docs merge;
DOC-10 is an asynchronous UAT item (real human, not an agent — do not simulate it)
that does NOT block advancing to Phase 4. This plan produces the checklist artifact
and opens the tracking issue; the actual human run happens asynchronously and the
issue closes when it documents a pass.

Output: a checklist markdown artifact + a GitHub issue opened from it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-bloque-c-documentaci-n-operacional/03-CONTEXT.md
@.planning/phases/03-bloque-c-documentaci-n-operacional/03-RESEARCH.md
@.planning/phases/03-bloque-c-documentaci-n-operacional/03-VALIDATION.md
@CONTRIBUTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the DOC-10 smoke-test checklist and open the GitHub issue</name>
  <files>.planning/phases/03-bloque-c-documentaci-n-operacional/DOC-10-smoke-checklist.md</files>
  <read_first>
    - CONTRIBUTING.md (the document the external dev follows — the checklist mirrors its setup steps; created in plan 03-05)
    - .planning/phases/03-bloque-c-documentaci-n-operacional/03-CONTEXT.md (D-16: dev externo real + checklist en GitHub issue; phase is implemented on merge, DOC-10 is async UAT, does not block Phase 4)
    - .planning/phases/03-bloque-c-documentaci-n-operacional/03-RESEARCH.md (Open Questions §2: 6-7 item checklist content; Anti-pattern: do not simulate as an agent checklist)
  </read_first>
  <action>
    Create .planning/phases/03-bloque-c-documentaci-n-operacional/DOC-10-smoke-checklist.md
    containing the issue body: a short intro ("external dev, no prior repo context, follows
    CONTRIBUTING.md only") and a GitHub-task-list checklist (per Open Questions §2):
    - [ ] git clone succeeds
    - [ ] `docker compose -f infra/docker-compose.yml up -d` exits clean
    - [ ] http://localhost:8069 reachable
    - [ ] DB creation works (Name l10n_py_dev, Country Paraguay, Demo NO)
    - [ ] l10n_py_base installs without error
    - [ ] l10n_py_account installs without error
    - [ ] Odoo test runner reports 97 green (the test command from CONTRIBUTING.md)
    Plus a `## Fricciones encontradas` free-text section for the tester to log friction.
    Then open the GitHub issue from this body using the gh CLI:
    `gh issue create --title "DOC-10: smoke test docs — dev externo" --body-file
    .planning/phases/03-bloque-c-documentaci-n-operacional/DOC-10-smoke-checklist.md`.
    If gh returns an auth error, STOP and surface an authentication checkpoint (do not fabricate
    an issue URL). Do NOT check any box yourself and do NOT close the issue — this is a real
    human async UAT (do not simulate). Record the created issue URL in the plan SUMMARY.
    Commit the checklist artifact: `docs(doc-10): add smoke-test checklist for async UAT`.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-bloque-c-documentaci-n-operacional/DOC-10-smoke-checklist.md && grep -q "docker compose" .planning/phases/03-bloque-c-documentaci-n-operacional/DOC-10-smoke-checklist.md && grep -q "97" .planning/phases/03-bloque-c-documentaci-n-operacional/DOC-10-smoke-checklist.md</automated>
  </verify>
  <acceptance_criteria>
    - DOC-10-smoke-checklist.md exists with a GitHub task-list (`- [ ]`) of ≥6 items covering clone, docker up, localhost reachable, DB creation, both module installs, and tests-run
    - The checklist references following CONTRIBUTING.md only (no extra context)
    - A GitHub issue was opened (issue URL recorded in SUMMARY) OR an auth checkpoint was raised if gh was unauthenticated
    - No checkbox is pre-checked; the issue is left open (async UAT, not simulated)
  </acceptance_criteria>
  <done>The DOC-10 smoke-test checklist exists and a tracking GitHub issue is opened for the real external-dev async UAT, left open and unchecked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm DOC-10 issue opened and acknowledge async UAT ownership</name>
  <files>(none — verification checkpoint)</files>
  <action>
    Pause for the maintainer to verify the DOC-10 tracking issue. This checkpoint confirms a
    real external-dev async UAT is queued — it does NOT perform the smoke test (a human does,
    asynchronously) and does NOT block Phase 4 (D-16). Present the issue URL from Task 1 and
    wait for the resume signal.
  </action>
  <what-built>
    A GitHub issue tracking the DOC-10 external-dev smoke test, plus the checklist
    artifact under the phase directory. Per D-16 the phase is "implemented" on doc merge;
    the human smoke test runs asynchronously and does not block Phase 4.
  </what-built>
  <how-to-verify>
    1. Open the GitHub issue URL recorded in the SUMMARY (or run `gh issue list --search "DOC-10"`).
    2. Confirm the issue body has the clone → docker up → DB → install → tests checklist.
    3. Acknowledge that recruiting the external dev + running the smoke test is an open async
       item tracked by this issue, NOT a blocker for `/gsd:execute-phase 4`.
  </how-to-verify>
  <verify>
    <human-check>Maintainer confirms the DOC-10 issue is open with the full checklist and acknowledges it does not block Phase 4.</human-check>
  </verify>
  <done>Maintainer has confirmed the open DOC-10 issue and accepted async-UAT ownership.</done>
  <resume-signal>Type "approved" (issue confirmed; async UAT acknowledged) or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary        | Description                                                     |
| --------------- | --------------------------------------------------------------- |
| gh CLI → GitHub | Opening an issue uses the maintainer's authenticated gh session |

## STRIDE Threat Register

| Threat ID | Category               | Component              | Disposition | Mitigation Plan                                                                                                                                                                       |
| --------- | ---------------------- | ---------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-03-12   | Spoofing               | Fabricated DOC-10 pass | mitigate    | The executor MUST NOT pre-check boxes or close the issue; a real external human performs the UAT (do not simulate). The checkpoint requires the maintainer to confirm the open issue. |
| T-03-13   | Information Disclosure | issue body content     | mitigate    | Checklist references only public dev compose + default dev creds (admin/admin, change on login); no production secrets                                                                |
| T-03-SC   | Tampering              | npm/pip/cargo installs | accept      | No package installs in this plan                                                                                                                                                      |

</threat_model>

<verification>
- `test -f .planning/phases/03-bloque-c-documentaci-n-operacional/DOC-10-smoke-checklist.md`
- GitHub issue exists (`gh issue list --search "DOC-10"` returns the issue) — manual/checkpoint
- Issue is open and unchecked (async UAT not simulated)
</verification>

<success_criteria>

- DOC-10 smoke-test checklist artifact created
- Tracking GitHub issue opened (or auth checkpoint raised), left open for real async UAT (DOC-10)
- Maintainer acknowledges DOC-10 does not block Phase 4
  </success_criteria>

<output>
Create `.planning/phases/03-bloque-c-documentaci-n-operacional/03-06-SUMMARY.md` when done (record the GitHub issue URL)
</output>
