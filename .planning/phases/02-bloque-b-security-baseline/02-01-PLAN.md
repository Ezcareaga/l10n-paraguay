---
phase: 02-bloque-b-security-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - SECURITY.md
autonomous: true
requirements:
  - SEC-01
  - SEC-02
risk_level: low
must_haves:
  goals:
    - "Quien clona el repo encuentra LICENSE AGPL-3.0 visible en raíz y SECURITY.md con canal privado de reporte de vulnerabilidades."
  truths:
    - "LICENSE existe en repo root con SHA256 == 0d96a4ff68ad6d4b6f1f30f713b18d5184912ba8dd389f86aa7710db079abcb0"
    - "SECURITY.md existe en repo root con secciones Supported Versions, Reporting a Vulnerability, Security Update Process, Acknowledgements"
    - "SECURITY.md referencia GitHub Security Advisories como canal primario y careagaezz@gmail.com como fallback"
    - "SECURITY.md NO contiene sección PGP (per D-06) ni tabla Hall of Fame manual (per D-07)"
    - "Los manifests __manifest__.py de l10n_py_base y l10n_py_account ya declaran license='AGPL-3' (consistencia preexistente verificada en RESEARCH.md §LICENSE Canonical Reference)"
  artifacts:
    - path: "LICENSE"
      provides: "Texto completo AGPL-3.0 (canonical de gnu.org)"
      contains: "GNU AFFERO GENERAL PUBLIC LICENSE"
    - path: "SECURITY.md"
      provides: "Política de seguridad y canal de reporte"
      contains: "## Reporting a Vulnerability"
  key_links:
    - from: "SECURITY.md"
      to: "https://github.com/Ezcareaga/l10n-paraguay/security/advisories/new"
      via: "markdown link"
      pattern: "security/advisories/new"
    - from: "addons/*/__manifest__.py"
      to: "LICENSE"
      via: "license='AGPL-3' field referencing repo root LICENSE"
      pattern: "license.*AGPL-3"
---

<objective>
Agregar los dos archivos de metadata visibles que cualquier clone del repo debe encontrar para entender (a) bajo qué licencia consumir el código (SEC-01) y (b) cómo reportar vulnerabilidades de forma privada (SEC-02). Cero código nuevo, cero workflows — sólo dos archivos en raíz alineados con texto canónico (GNU AGPL-3.0) y skeleton ya definido en RESEARCH.md.

Purpose: Cumplir los dos REQs más visibles de Bloque B sin tocar CI todavía, dejando el repo presentable para reviewers OCA y reporters de vulnerabilidades antes de activar workflows de scanning.

Output: 2 archivos (`LICENSE`, `SECURITY.md`), 1 commit `docs(sec): add LICENSE AGPL-3.0 and SECURITY.md` o 2 commits separados (`feat(sec): add LICENSE AGPL-3.0` + `docs(sec): add SECURITY.md vulnerability disclosure policy`), 1 PR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-bloque-b-security-baseline/02-CONTEXT.md
@.planning/phases/02-bloque-b-security-baseline/02-RESEARCH.md
@.planning/phases/02-bloque-b-security-baseline/02-PATTERNS.md

# Manifest consistency confirmation (already correct — do NOT modify)

@addons/l10n_py_base/**manifest**.py
@addons/l10n_py_account/**manifest**.py
@pyproject.toml
</context>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                                                      |
| --------------------- | -------------------------------------------------------------------------------- |
| external → repo       | Anyone cloning the repo (potential consumer / reviewer / vulnerability reporter) |
| reporter → maintainer | Security researcher reporting a vulnerability privately                          |

## STRIDE Threat Register

| Threat ID              | Category               | Component              | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                  |
| ---------------------- | ---------------------- | ---------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-SEC-01-license       | Repudiation            | repo root              | mitigate    | Add canonical AGPL-3.0 LICENSE file in repo root (Task 02-01-01); enforce AGPL terms become possible because the license is unambiguously present and matches `__manifest__.py` `license="AGPL-3"` already in both addons                                                        |
| T-SEC-02-disc          | Information Disclosure | Vulnerability reports  | mitigate    | Add SECURITY.md (Task 02-01-02) pointing primary channel to GitHub Security Advisories (private, encrypted in transit, CVE-ready) per D-05; fallback `careagaezz@gmail.com` only for reporters without GH account; no PGP key required per D-06 (low report volume threat model) |
| T-SEC-02-impersonation | Spoofing               | Email fallback channel | accept      | SECURITY.md notes "email is plaintext; do not include production secrets in initial report"; risk accepted per D-06 rationale (one maintainer, expected low volume); reopen if threat model changes                                                                              |

</threat_model>

<tasks>

<task id="02-01-01" type="auto">
  <name>Task 02-01-01: Download canonical AGPL-3.0 LICENSE and verify SHA256</name>
  <files>LICENSE</files>
  <read_first>
    - .planning/phases/02-bloque-b-security-baseline/02-CONTEXT.md (D-01..D-15 + amendments)
    - .planning/phases/02-bloque-b-security-baseline/02-RESEARCH.md §LICENSE Canonical Reference (URL + SHA256 + manifest consistency confirmation)
    - .planning/phases/02-bloque-b-security-baseline/02-PATTERNS.md §LICENSE (download + verify procedure, pitfalls)
    - addons/l10n_py_base/__manifest__.py (confirms license="AGPL-3" already present — do NOT modify)
    - addons/l10n_py_account/__manifest__.py (idem)
    - pyproject.toml (confirms license={text="AGPL-3.0"} already present — do NOT modify)
  </read_first>
  <action>
    Download the canonical AGPL-3.0 text from `https://www.gnu.org/licenses/agpl-3.0.txt` and save as `LICENSE` in the repo root. Use UTF-8, no BOM. Verify the SHA256 of the resulting file equals exactly `0d96a4ff68ad6d4b6f1f30f713b18d5184912ba8dd389f86aa7710db079abcb0` (per RESEARCH.md §LICENSE Canonical Reference). If SHA256 mismatches, abort — do NOT commit a file with a different hash. Use the `.txt` endpoint, NOT `agpl-3.0.en.html` (HTML formatting differs). Do NOT edit the file after download. Do NOT modify `addons/*/__manifest__.py` or `pyproject.toml` — they are already consistent (see 02-RESEARCH.md §LICENSE Canonical Reference for verification of the three lines: `license="AGPL-3"` in both manifests + `license={text="AGPL-3.0"}` in pyproject.toml).

    Suggested PowerShell command:
    `Invoke-WebRequest -Uri https://www.gnu.org/licenses/agpl-3.0.txt -OutFile LICENSE; (Get-FileHash LICENSE -Algorithm SHA256).Hash.ToLower()`

    Commit with Conventional Commits prefix: `feat(sec): add LICENSE AGPL-3.0 (closes SEC-01)`.

  </action>
  <acceptance_criteria>
    - `Test-Path LICENSE` returns `True` (file exists in repo root)
    - `(Get-FileHash LICENSE -Algorithm SHA256).Hash.ToLower()` equals `0d96a4ff68ad6d4b6f1f30f713b18d5184912ba8dd389f86aa7710db079abcb0`
    - `Get-Item LICENSE | Select-Object -ExpandProperty Length` returns 34523 (bytes)
    - `Select-String -Path LICENSE -Pattern 'GNU AFFERO GENERAL PUBLIC LICENSE' -Quiet` returns `True`
    - `Select-String -Path LICENSE -Pattern 'Version 3, 19 November 2007' -Quiet` returns `True`
    - `Select-String -Path addons\l10n_py_base\__manifest__.py -Pattern '"license":\s*"AGPL-3"'` returns 1 match (preexisting — verify not regressed)
    - `Select-String -Path addons\l10n_py_account\__manifest__.py -Pattern '"license":\s*"AGPL-3"'` returns 1 match (preexisting — verify not regressed)
    - `git diff HEAD~1 addons/ pyproject.toml | Measure-Object -Line` returns 0 lines added/removed (no manifest or pyproject changes)
    - Commit message follows Conventional Commits: `feat(sec): add LICENSE AGPL-3.0` (or equivalent with `(closes SEC-01)` trailer)
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "(Get-FileHash LICENSE -Algorithm SHA256).Hash.ToLower() -eq '0d96a4ff68ad6d4b6f1f30f713b18d5184912ba8dd389f86aa7710db079abcb0'"</automated>
  </verify>
  <done>LICENSE file present in repo root with canonical SHA256; manifests untouched.</done>
</task>

<task id="02-01-02" type="auto">
  <name>Task 02-01-02: Create SECURITY.md with vulnerability disclosure policy</name>
  <files>SECURITY.md</files>
  <read_first>
    - .planning/phases/02-bloque-b-security-baseline/02-CONTEXT.md §"SECURITY.md mecánica de reporte" (D-05, D-06, D-07, D-08)
    - .planning/phases/02-bloque-b-security-baseline/02-RESEARCH.md §SECURITY.md Skeleton (use this skeleton verbatim)
    - .planning/phases/02-bloque-b-security-baseline/02-PATTERNS.md §SECURITY.md (style anchor + pitfalls)
    - README.md (existing style anchor: H1 + bold-metadata + H2 sections + tables — do NOT modify README in this task)
  </read_first>
  <action>
    Create `SECURITY.md` in the repo root with the exact skeleton from 02-RESEARCH.md §SECURITY.md Skeleton. Sections in order:

    1. `# Security Policy` (H1)
    2. `## Supported Versions` table with rows `18.0.x | ✅` and `< 18.0 | ❌` + the sentence "Only the latest minor release of the 18.0 series receives security fixes." (D-08)
    3. `## Reporting a Vulnerability` containing:
       - Preferred channel link: `https://github.com/Ezcareaga/l10n-paraguay/security/advisories/new` (D-05, ends in `/new`)
       - Fallback email: `careagaezz@gmail.com` with subject convention `[SECURITY] <brief description>` + the plaintext warning
       - Response SLA bulleted list: 72h confirmation / 30d fix critical-high / best-effort lower (D-05)
    4. `## Security Update Process` numbered 1–5 steps from the skeleton
    5. `## Acknowledgements` linking to `https://github.com/Ezcareaga/l10n-paraguay/security/advisories` (D-07, public listing, NO `/new` suffix)

    DO NOT add a PGP section (D-06). DO NOT add a Hall of Fame table (D-07). DO NOT mention any regulatory authority (that belongs in docs/61, not here). The GH username in URLs is `@Ezcareaga` — verified against `__manifest__.py` website field.

    Commit with Conventional Commits prefix: `docs(sec): add SECURITY.md with private vulnerability disclosure (closes SEC-02)`.

  </action>
  <acceptance_criteria>
    - `Test-Path SECURITY.md` returns `True`
    - `Select-String -Path SECURITY.md -Pattern '^# Security Policy$' -Quiet` returns `True` (H1 present)
    - `Select-String -Path SECURITY.md -Pattern '## Supported Versions' -Quiet` returns `True`
    - `Select-String -Path SECURITY.md -Pattern '## Reporting a Vulnerability' -Quiet` returns `True`
    - `Select-String -Path SECURITY.md -Pattern '## Security Update Process' -Quiet` returns `True`
    - `Select-String -Path SECURITY.md -Pattern '## Acknowledgements' -Quiet` returns `True`
    - `Select-String -Path SECURITY.md -Pattern 'security/advisories/new'` returns ≥1 match (primary reporting URL, D-05)
    - `Select-String -Path SECURITY.md -Pattern 'careagaezz@gmail.com'` returns ≥1 match (fallback channel, D-05)
    - `Select-String -Path SECURITY.md -Pattern '18\.0\.x'` returns ≥1 match (D-08 supported version)
    - `Select-String -Path SECURITY.md -Pattern '72 hours'` returns ≥1 match (SLA D-05)
    - `Select-String -Path SECURITY.md -Pattern '30 days'` returns ≥1 match (SLA D-05)
    - `(Select-String -Path SECURITY.md -Pattern '(?i)PGP|GPG|public.key' -CaseSensitive:$false | Measure-Object).Count` returns 0 (D-06: no PGP)
    - `(Select-String -Path SECURITY.md -Pattern '(?i)Hall.of.Fame' | Measure-Object).Count` returns 0 (D-07: no manual HoF table)
    - `(Select-String -Path SECURITY.md -Pattern '(?i)SENAC|ANPDP|MITIC' | Measure-Object).Count` returns 0 (regulator names live in docs/61, not here)
    - Commit message follows Conventional Commits: `docs(sec): add SECURITY.md ...`
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$ok = (Test-Path SECURITY.md) -and ((Select-String -Path SECURITY.md -Pattern 'security/advisories/new' -Quiet) -eq $true) -and ((Select-String -Path SECURITY.md -Pattern 'careagaezz@gmail.com' -Quiet) -eq $true) -and (((Select-String -Path SECURITY.md -Pattern '(?i)PGP|GPG' | Measure-Object).Count) -eq 0); if (-not $ok) { exit 1 }"</automated>
  </verify>
  <done>SECURITY.md present in repo root with skeleton intact, no PGP, no manual HoF, no regulator references.</done>
</task>

</tasks>

<verification_criteria>
Plan 01 succeeds when:

- LICENSE exists with canonical SHA256
- SECURITY.md exists with the 4 required H2 sections + correct links + no PGP/HoF/regulator content
- `pre-commit run --files LICENSE SECURITY.md` passes (no formatting violations on new files)
- Manifests and pyproject.toml unchanged (consistency preserved)
- 1 PR opened, both commits pass `commitlint` and `pre-commit` workflows in CI (Phase 1 infra already active)
- After merge to `main`, GitHub renders SECURITY.md in the Security tab and shows the "Report a vulnerability" affordance
  </verification_criteria>

<success_criteria>

- SEC-01 closed: LICENSE in root, manifests already consistent
- SEC-02 closed: SECURITY.md describes channels + SLA + support window + acknowledgements
- No regression in CI: pre-commit, tests, commitlint workflows from Phase 1 stay green
- Branch protection on `main` (active since Phase 1) enforces PR merge — no direct push
  </success_criteria>

<output>
Create `.planning/phases/02-bloque-b-security-baseline/02-01-SUMMARY.md` when done, summarizing: files added, SHA256 verification result, links to PR.
</output>
