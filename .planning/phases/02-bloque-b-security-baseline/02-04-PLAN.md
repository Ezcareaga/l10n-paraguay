---
phase: 02-bloque-b-security-baseline
plan: 04
type: execute
wave: 4
depends_on:
  - 02-02
files_modified:
  - docs/60_SECURITY_BASELINE.md
  - scripts/restore-smoke.sh
  - README.md
autonomous: true
requirements:
  - SEC-06
risk_level: low
must_haves:
  goals:
    - "Existe un documento operacional `docs/60_SECURITY_BASELINE.md` que documenta los 6 ejes de la baseline de seguridad (auth/2FA, password policy, audit logs, backup, CCFE encryption, network security) con comandos ilustrativos marcados como 'validar en Pre-Fase 3', listo para que Fase 2 EDI implemente el helper CCFE sin re-decidir."
  truths:
    - "docs/60_SECURITY_BASELINE.md existe con 6 secciones H2 cubriendo los 6 ejes en este orden: Autenticación y 2FA, Política de contraseñas, Audit logs (OCA auditlog), Backup strategy, CCFE encryption blueprint, Seguridad de red"
    - "El eje CCFE encryption incluye el blueprint Fernet + MultiFernet completo (D-10): envelope schema, encrypt/decrypt/rotate pseudo-código, systemd-creds para master key, ir.config_parameter para wrap_key_id"
    - "El eje Audit logs (D-11) lista los modelos a auditar: res.partner (vat, l10n_py_dv, l10n_latam_identification_type_id), res.company (vat), l10n_py.timbrado (name, state, expiry_date), l10n_latam.document.type (code, internal_type), account.move (post-Fase 2 EDI)"
    - "El eje Audit logs documenta retention = 7 años archivado + 1 año online (D-11, Ley 125/91 PY base)"
    - "El eje Backup (D-12) documenta pg_dump local + Backblaze B2 offsite + monthly restore test referenciando scripts/restore-smoke.sh"
    - "Cada snippet de comando lleva el marker `> Note: validar en Pre-Fase 3 cuando exista deploy real` (D-09)"
    - "scripts/restore-smoke.sh existe como stub ejecutable con shebang bash + set -euo pipefail + exit 0"
    - "README.md tiene 1 nuevo badge de security workflow y 1 nueva sección 'Seguridad' (2 líneas) — no refactor completo del README (eso es Phase 3 DOC-01)"
    - "docs/60 NO contiene código Fernet real ejecutable — todo es blueprint marcado como 'NO ejecutar en Phase 2'"
  artifacts:
    - path: "docs/60_SECURITY_BASELINE.md"
      provides: "Baseline de seguridad operacional con 6 ejes y blueprint CCFE para Fase 2 EDI"
      contains: "## 5. CCFE encryption blueprint"
      min_lines: 200
    - path: "scripts/restore-smoke.sh"
      provides: "Stub ejecutable del monthly restore test (implementación completa en Pre-Fase 3)"
      contains: "STUB"
    - path: "README.md"
      provides: "Modificado: 1 badge security + 1 sección Seguridad"
      contains: "security.yml/badge.svg"
  key_links:
    - from: "docs/60_SECURITY_BASELINE.md §3 Audit logs"
      to: "docs/61_COMPLIANCE_LEY_7593.md §3 ARCO + mecanismos Odoo"
      via: "cross-reference markdown link"
      pattern: "61_COMPLIANCE_LEY_7593"
    - from: "docs/60_SECURITY_BASELINE.md §4 Backup strategy"
      to: "scripts/restore-smoke.sh"
      via: "markdown link to script path"
      pattern: "scripts/restore-smoke\\.sh"
    - from: "README.md badges block"
      to: ".github/workflows/security.yml"
      via: "security workflow badge URL"
      pattern: "actions/workflows/security\\.yml/badge"
---

<objective>
Escribir el documento operacional `docs/60_SECURITY_BASELINE.md` que sirve como blueprint reusable para la baseline de seguridad cuando exista deploy real (Pre-Fase 3) y, crucialmente, como spec implementable para el helper Fernet de CCFE en Fase 2 EDI. Crear el stub `scripts/restore-smoke.sh` referenciado por el eje Backup. Modificación mínima a README.md (1 badge + 1 sección de 2 líneas) — el refactor real del README es Phase 3 DOC-01.

Purpose: Cerrar SEC-06 con un doc que sea suficientemente denso para que Fase 2 EDI escriba el código de crypto sin re-decidir (D-10: "scope creep escribirlo ahora; el doc tiene que ser tan claro que cuando Fase 2 EDI lo lea, el código se escribe siguiendo el patrón").

Output: 3 archivos (1 nuevo doc denso `docs/60_*`, 1 stub `scripts/restore-smoke.sh`, 1 modify mínimo a README.md), 1-3 commits separados (recomendado: `docs(sec): add security baseline doc (closes SEC-06)` + `feat(sec): add restore-smoke.sh stub` + `docs(sec): add security badge + section to README`), 1 PR. Plan paralelo con Plan 05 (ambos son docs, sin file overlap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-bloque-b-security-baseline/02-CONTEXT.md
@.planning/phases/02-bloque-b-security-baseline/02-RESEARCH.md
@.planning/phases/02-bloque-b-security-baseline/02-PATTERNS.md

# Style anchors for the new doc

@docs/60_FASE_1_RETROSPECTIVA.md
@docs/03_DOMAIN_MODEL.md
@docs/01_SIFEN_KNOWLEDGE_BASE.md

# Existing assets to preserve / reference

@README.md
@scripts/setup_references.sh
@addons/l10n_py_base/models/res_partner.py
@addons/l10n_py_base/models/res_company.py
@addons/l10n_py_account/models/l10n_py_timbrado.py
</context>

<threat_model>

## Trust Boundaries

| Boundary                                         | Description                                                                                   |
| ------------------------------------------------ | --------------------------------------------------------------------------------------------- |
| operator / future maintainer → security playbook | Doc must be specific enough that there is no ambiguity at deploy time                         |
| Phase 2 EDI implementer → CCFE crypto spec       | The blueprint is a contract: encrypt/decrypt/rotate signatures must be implementable verbatim |

## STRIDE Threat Register

| Threat ID               | Category                             | Component               | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                       |
| ----------------------- | ------------------------------------ | ----------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-SEC-06-doc            | Information Disclosure / Repudiation | operator deployment     | mitigate    | docs/60 documents 6 ejes with snippets (Task 02-04-01); without this, operador deploys without 2FA / audit logs / encrypted CCFE / backup strategy / firewall rules and there is no consolidated spec to point to                                                                                     |
| T-SEC-06-ccfe-blueprint | Tampering                            | future CCFE crypto code | mitigate    | The CCFE blueprint section (D-10) is dense enough that Fase 2 EDI implements `l10n_py_edi.tools.crypto` following the pattern without re-deciding envelope schema, rotation cadence, or storage layout. Includes Fernet+MultiFernet pseudo-code, systemd-creds invocation, ir.config_parameter layout |
| T-SEC-06-stub           | Information Disclosure               | restore-smoke.sh stub   | accept      | Stub exits 0 with a printed message. Risk that someone runs it expecting real behavior = low (comment makes it clear it's a stub). Full impl deferred to Pre-Fase 3 per D-12 + Deferred                                                                                                               |

</threat_model>

<tasks>

<task id="02-04-01" type="auto">
  <name>Task 02-04-01: Author docs/60_SECURITY_BASELINE.md with 6 ejes + CCFE blueprint</name>
  <files>docs/60_SECURITY_BASELINE.md</files>
  <read_first>
    - .planning/phases/02-bloque-b-security-baseline/02-CONTEXT.md §"docs/60 alcance + CCFE encryption" (D-09 commands marker, D-10 CCFE blueprint, D-11 audit logs, D-12 backup)
    - .planning/phases/02-bloque-b-security-baseline/02-RESEARCH.md §"CCFE Fernet Blueprint Detail" (envelope schema, encrypt/decrypt/rotate pseudo-code, systemd-creds, ir.config_parameter — paste verbatim into the doc with the "NO ejecutar en Phase 2" marker)
    - .planning/phases/02-bloque-b-security-baseline/02-RESEARCH.md §"OCA auditlog 18.0 Status" (list of models to audit, table format ready to paste)
    - .planning/phases/02-bloque-b-security-baseline/02-PATTERNS.md §"docs/60_SECURITY_BASELINE.md" (section structure, style anchors, pitfalls)
    - docs/60_FASE_1_RETROSPECTIVA.md (style anchor — H1 + bold metadata + `---` + H2 sections; NO YAML frontmatter)
    - docs/03_DOMAIN_MODEL.md (table-heavy structured doc style anchor)
    - addons/l10n_py_base/models/res_partner.py (confirms sensitive field names: vat, l10n_py_dv, l10n_latam_identification_type_id — do NOT modify)
    - addons/l10n_py_account/models/l10n_py_timbrado.py (confirms l10n_py.timbrado model name + field list — do NOT modify)
  </read_first>
  <action>
    Create `docs/60_SECURITY_BASELINE.md` (no YAML frontmatter — only docs/01 has legacy frontmatter). Use the style anchor from `docs/60_FASE_1_RETROSPECTIVA.md`:

    ```
    # Security Baseline — l10n-paraguay

    **Estado:** baseline pre-deploy (Pre-Fase 2 Bloque B)
    **Vigencia:** Pre-Fase 3 — cuando exista deploy real, validar comandos contra entorno
    **Cross-ref:** ver [`docs/61_COMPLIANCE_LEY_7593.md`](61_COMPLIANCE_LEY_7593.md) para alcance legal

    ---
    ```

    Then 6 secciones H2 EN ESTE ORDEN (per D-09 + PATTERNS.md):

    1. `## 1. Autenticación y 2FA` — política para admin de Odoo: forzar 2FA con OCA `auth_totp_mail_enforce` (verificar disponibilidad 18.0, sino documentar como gap), longitud mínima de password admin = 14, password rotation 90d. Comandos ilustrativos: `odoo-bin --load=base,web,auth_totp` con marker.

    2. `## 2. Política de contraseñas` — para usuarios estándar: longitud ≥ 10, no diccionario, lockout tras 5 intentos. Apuntar al módulo `auth_password_policy` Odoo standard si existe en 18.0, sino documentar como gap.

    3. `## 3. Audit logs (OCA `auditlog`)` — incluir literal la **tabla de modelos** de 02-RESEARCH.md §"OCA auditlog 18.0 Status" "Lista de modelos a auditar":

       | Modelo | Módulo | Campos sensibles |
       | --- | --- | --- |
       | `res.partner` | `l10n_py_base` | `vat`, `l10n_py_dv`, `l10n_latam_identification_type_id` |
       | `res.company` | `l10n_py_base` | `vat`, `l10n_py_dv` |
       | `l10n_py.timbrado` | `l10n_py_account` | `name`, `state`, `expiry_date` |
       | `l10n_latam.document.type` | `l10n_py_account` | `code`, `internal_type` |
       | `account.move` | Odoo core | `state`, `name`, `amount_total` (post-Fase 2 EDI) |

       Indicar retention = **7 años archivado + 1 año online** (D-11 + Ley 125/91 PY base). Aclarar que `auditlog` NO se instala en Phase 2 — se suma al `__manifest__.py` del addon correspondiente en su phase (probable Fase 2 EDI). Incluir cross-ref a docs/61 §3 ARCO.

    4. `## 4. Backup strategy` — implementar D-12:

       - **Filesystem local:** `pg_dump | xz` diario, retención 7d, path `/var/backups/odoo/`. Snippet de cron.
       - **Offsite:** sync nocturno a Backblaze B2 (default por costo; AWS S3 si cliente paga). Retención offsite: 90d. Documentar ambos endpoints.
       - **Monthly restore test:** referenciar `scripts/restore-smoke.sh` (stub creado en Task 02-04-02). El script restaura el último dump a un container Postgres efímero y verifica `SELECT count(*) FROM ir_module_module WHERE state='installed'`.

       Cada snippet lleva el marker `> Note: validar en Pre-Fase 3 cuando exista deploy real` (D-09).

    5. `## 5. CCFE encryption blueprint (D-10)` — esta sección es la MÁS densa: paste verbatim de 02-RESEARCH.md §"CCFE Fernet Blueprint Detail" (Conceptos CCFE relevantes + Envelope schema + Pseudo-código Python + systemd-creds + ir.config_parameter + Rotation script outline). El bloque Python lleva el comentario `# Este bloque es blueprint para l10n_py_edi.tools.crypto — NO ejecutar en Phase 2`. Aclarar que el código real vive en `l10n_py_edi` (Fase 2 EDI). Incluir cross-ref a docs/61 §2 Vendor vs operador.

    6. `## 6. Seguridad de red` — VPS firewall (ufw rule outlines), fail2ban rule outline, Caddy reverse proxy SSL automático (cross-ref futuro docs/71 Phase 3). Cada snippet con marker `> Note: validar en Pre-Fase 3`.

    Estilo:

    - Idioma: español para prosa, inglés para identificadores de código.
    - Comentarios en código: español (project CLAUDE.md convention).
    - Cada bloque de código lleva tag de lenguaje (` ```bash `, ` ```python `, ` ```yaml `).
    - Sin YAML frontmatter (NO añadir — solo docs/01 tiene legacy frontmatter).
    - Tablas + bullets + code blocks > prosa larga (D-09 + specifics CONTEXT.md).

    NO cite SENAC anywhere (autoridad correcta = ANPDP, pero las referencias legales detalladas viven en docs/61 — desde acá solo cross-ref a docs/61).
    NO escribir el helper Fernet como implementación real — marcar claramente como blueprint stub.
    NO documentar `gdpr_purge` (no está en OCA 18.0) — usar `privacy_partner_to_be_forgotten` si se menciona privacy modules.

    Commit message: `docs(sec): add security baseline doc with 6 ejes + CCFE blueprint (closes SEC-06)`.

  </action>
  <acceptance_criteria>
    - `Test-Path docs/60_SECURITY_BASELINE.md` returns `True`
    - File lacks YAML frontmatter: first non-empty line starts with `# ` (H1), NOT `---`
    - All 6 H2 sections present: `Select-String -Path docs/60_SECURITY_BASELINE.md -Pattern '^## 1\. Autenticación y 2FA$|^## 2\. Política de contraseñas$|^## 3\. Audit logs|^## 4\. Backup|^## 5\. CCFE|^## 6\. Seguridad de red' | Measure-Object | Select-Object -ExpandProperty Count` equals 6
    - Audit log models table includes all 5 model rows: `Select-String -Path docs/60_SECURITY_BASELINE.md -Pattern '\bres\.partner\b|\bres\.company\b|\bl10n_py\.timbrado\b|\bl10n_latam\.document\.type\b|\baccount\.move\b' -AllMatches | ForEach-Object { $_.Matches.Count } | Measure-Object -Sum | Select-Object -ExpandProperty Sum` ≥ 5
    - Retention wording present: `Select-String -Path docs/60_SECURITY_BASELINE.md -Pattern '7 años' -Quiet` returns `True` AND `Select-String -Path docs/60_SECURITY_BASELINE.md -Pattern '1 año' -Quiet` returns `True`
    - CCFE blueprint markers present: `Select-String -Path docs/60_SECURITY_BASELINE.md -Pattern 'Fernet|MultiFernet' -Quiet` returns `True` AND `Select-String -Path docs/60_SECURITY_BASELINE.md -Pattern 'NO ejecutar en Phase 2' -Quiet` returns `True` AND `Select-String -Path docs/60_SECURITY_BASELINE.md -Pattern 'systemd-creds' -Quiet` returns `True` AND `Select-String -Path docs/60_SECURITY_BASELINE.md -Pattern 'ir\.config_parameter' -Quiet` returns `True`
    - D-09 marker on snippets: `(Select-String -Path docs/60_SECURITY_BASELINE.md -Pattern 'validar en Pre-Fase 3' | Measure-Object).Count` ≥ 4 (one per axis with operational snippets — at least axes 1, 4, 5, 6)
    - No SENAC mention: `(Select-String -Path docs/60_SECURITY_BASELINE.md -Pattern 'SENAC' | Measure-Object).Count` equals 0
    - No deprecated module name: `(Select-String -Path docs/60_SECURITY_BASELINE.md -Pattern 'gdpr_purge' | Measure-Object).Count` equals 0
    - Cross-ref to docs/61 present: `Select-String -Path docs/60_SECURITY_BASELINE.md -Pattern '61_COMPLIANCE_LEY_7593' -Quiet` returns `True`
    - Cross-ref to scripts/restore-smoke.sh present: `Select-String -Path docs/60_SECURITY_BASELINE.md -Pattern 'scripts/restore-smoke\.sh' -Quiet` returns `True`
    - Line count signals density: `(Get-Content docs/60_SECURITY_BASELINE.md | Measure-Object -Line).Lines` ≥ 200
    - `pre-commit run --files docs/60_SECURITY_BASELINE.md` exits 0 (prettier + codespell + yamllint pass)
    - Commit message follows Conventional Commits: `docs(sec): add security baseline doc ...`
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$f = 'docs/60_SECURITY_BASELINE.md'; $secs = (Select-String -Path $f -Pattern '^## [1-6]\. ' | Measure-Object).Count; $hasCCFE = (Select-String -Path $f -Pattern 'NO ejecutar en Phase 2' -Quiet); $noSenac = (((Select-String -Path $f -Pattern 'SENAC' | Measure-Object).Count) -eq 0); $ok = ($secs -ge 6) -and $hasCCFE -and $noSenac; if (-not $ok) { Write-Host (\"secs=$secs hasCCFE=$hasCCFE noSenac=$noSenac\"); exit 1 }"</automated>
  </verify>
  <done>docs/60_SECURITY_BASELINE.md complete with 6 ejes, CCFE blueprint dense enough for Fase 2 EDI to implement, cross-refs to docs/61 and restore-smoke.sh present.</done>
</task>

<task id="02-04-02" type="auto">
  <name>Task 02-04-02: Create scripts/restore-smoke.sh stub (executable, exits 0 with informational message)</name>
  <files>scripts/restore-smoke.sh</files>
  <read_first>
    - .planning/phases/02-bloque-b-security-baseline/02-CONTEXT.md D-12 (backup strategy) + §Deferred ("Implementar scripts/restore-smoke.sh ejecutable — Pre-Fase 3 cuando exista backend de backup real. Phase 2 deja el stub + el checklist")
    - .planning/phases/02-bloque-b-security-baseline/02-PATTERNS.md §"scripts/restore-smoke.sh" (full stub content + style pattern + adaptations)
    - scripts/setup_references.sh (style anchor: shebang, set -euo pipefail, SCRIPT_DIR/REPO_ROOT pattern)
  </read_first>
  <action>
    Create `scripts/restore-smoke.sh` using the stub body from 02-PATTERNS.md §"scripts/restore-smoke.sh" "Stub content for restore-smoke.sh". Required elements:

    - Shebang: `#!/usr/bin/env bash`
    - Top-of-file comment block in Spanish (project CLAUDE.md convention) describing:
      - Purpose (D-12 D-09 monthly restore test stub)
      - Status: STUB — implementation completa en Pre-Fase 3
      - Reference to `docs/60_SECURITY_BASELINE.md §4 Backup strategy`
      - The 5 steps the real script will perform (xz -d → docker run postgres:12 → psql restore → SELECT count → docker rm)
    - `set -euo pipefail`
    - `SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"` + `REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"` boilerplate
    - Two `echo` lines: one announcing it is a STUB, one pointing to docs/60 for the design
    - `exit 0` so pre-commit and CI don't fail if invoked

    The script must parse syntactically: `bash -n scripts/restore-smoke.sh` must return exit 0.

    Mark executable: on git side use `git update-index --add --chmod=+x scripts/restore-smoke.sh` (Windows git preserves the +x bit for shell scripts even though NTFS doesn't have a Unix executable concept).

    Commit message: `feat(sec): add scripts/restore-smoke.sh stub for monthly restore test (D-12)`.

  </action>
  <acceptance_criteria>
    - `Test-Path scripts/restore-smoke.sh` returns `True`
    - First line of file equals `#!/usr/bin/env bash`
    - `Select-String -Path scripts/restore-smoke.sh -Pattern 'STUB' -Quiet` returns `True`
    - `Select-String -Path scripts/restore-smoke.sh -Pattern 'set -euo pipefail' -Quiet` returns `True`
    - `Select-String -Path scripts/restore-smoke.sh -Pattern 'docs/60_SECURITY_BASELINE\.md' -Quiet` returns `True`
    - `Select-String -Path scripts/restore-smoke.sh -Pattern 'exit 0' -Quiet` returns `True`
    - Syntactically valid: `bash -n scripts/restore-smoke.sh` exits 0 (run via Git Bash on Windows)
    - Git executable bit set: `git ls-files --stage scripts/restore-smoke.sh` shows mode `100755` (not `100644`)
    - `pre-commit run --files scripts/restore-smoke.sh` exits 0
    - Commit message follows Conventional Commits: `feat(sec): add scripts/restore-smoke.sh stub ...`
  </acceptance_criteria>
  <verify>
    <automated>bash -c "bash -n scripts/restore-smoke.sh && grep -q '^#!/usr/bin/env bash' scripts/restore-smoke.sh && grep -q 'STUB' scripts/restore-smoke.sh && grep -q 'exit 0' scripts/restore-smoke.sh"</automated>
  </verify>
  <done>scripts/restore-smoke.sh exists as executable stub with proper boilerplate, references docs/60, exits 0.</done>
</task>

<task id="02-04-03" type="auto">
  <name>Task 02-04-03: Add security workflow badge + "Seguridad" section to README.md (minimal touch)</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/02-bloque-b-security-baseline/02-PATTERNS.md §"README.md (root-meta, MODIFY)" (existing badge block, exact new badge string, exact new "Seguridad" section, pitfalls including DO NOT refactor)
    - README.md (current state — already has 5 badges in lines 3-7 and a "Licencia" section at line 81)
    - .github/workflows/security.yml (the workflow whose badge we are adding — must already be merged to main per dependency on Plan 02; in pre-merge state the badge will show "no status" until first run)
  </read_first>
  <action>
    Modify `README.md` with EXACTLY two changes — nothing else (full README refactor is Phase 3 DOC-01):

    1. **Add 1 badge** AFTER the existing `pre-commit` badge (line 4) and BEFORE the `codecov` badge (current line 5). Use the exact GitHub Actions badge format that matches lines 3-4:

       ```
       [![Security](https://github.com/Ezcareaga/l10n-paraguay/actions/workflows/security.yml/badge.svg?branch=main)](https://github.com/Ezcareaga/l10n-paraguay/actions/workflows/security.yml)
       ```

       This preserves the visual grouping: CI workflows badges first (test, pre-commit, security), then external service badges (codecov), then static info badges (license, Odoo).

    2. **Add 1 new H2 section** named "Seguridad" placed JUST BEFORE the existing `## Licencia` section (current line 81), with exactly 2 lines of content:

       ```markdown
       ## Seguridad

       Para reportar vulnerabilidades, ver [`SECURITY.md`](SECURITY.md).
       El workflow de seguridad (gitleaks + Bandit + Dependency Review) corre en cada PR.
       ```

    DO NOT change ANY other line of README.md. DO NOT rewrite the existing "TODO" rows in the Módulos planificados table (Phase 3 DOC-01 handles that). DO NOT remove the trailing "Estado" section. DO NOT change badge ordering of existing badges.

    Commit message: `docs(sec): add security workflow badge and Seguridad section to README (SEC-06)`.

  </action>
  <acceptance_criteria>
    - `Select-String -Path README.md -Pattern 'actions/workflows/security\.yml/badge\.svg'` returns exactly 1 match (new badge)
    - The new badge is positioned between the `pre-commit` badge and the `codecov` badge: `(Get-Content README.md) | Select-String -Pattern '(pre-commit\.yml/badge|security\.yml/badge|codecov\.io/gh)' | ForEach-Object { $_.LineNumber }` returns 3 consecutive ascending line numbers
    - `Select-String -Path README.md -Pattern '^## Seguridad$' -Quiet` returns `True`
    - Seguridad section content present: `Select-String -Path README.md -Pattern 'reportar vulnerabilidades.*SECURITY\.md' -Quiet` returns `True`
    - "Licencia" H2 still present and unchanged: `Select-String -Path README.md -Pattern '^## Licencia$' -Quiet` returns `True`
    - "Seguridad" appears before "Licencia": `((Get-Content README.md) | Select-String -Pattern '^## Seguridad$').LineNumber -lt ((Get-Content README.md) | Select-String -Pattern '^## Licencia$').LineNumber` returns `True`
    - No other H2 sections added/removed: `(Select-String -Path README.md -Pattern '^## ' | Measure-Object).Count` matches existing count + 1 (current README has 6 H2 sections; after this change must be 7)
    - Existing badge count = old + 1: `(Select-String -Path README.md -Pattern '^\[!\[' | Measure-Object).Count` equals 6 (was 5: CI, pre-commit, codecov, License, Odoo → now 6 with Security)
    - `pre-commit run --files README.md` exits 0
    - Commit message follows Conventional Commits: `docs(sec): add security workflow badge ...`
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$f = 'README.md'; $badgeCnt = (Select-String -Path $f -Pattern '^\[!\[' | Measure-Object).Count; $hasSec = (Select-String -Path $f -Pattern '^## Seguridad$' -Quiet); $hasSecBadge = (Select-String -Path $f -Pattern 'security\.yml/badge\.svg' -Quiet); $ok = ($badgeCnt -eq 6) -and $hasSec -and $hasSecBadge; if (-not $ok) { Write-Host (\"badgeCnt=$badgeCnt hasSec=$hasSec hasSecBadge=$hasSecBadge\"); exit 1 }"</automated>
  </verify>
  <done>README.md has exactly 1 new badge + 1 new section, all other content untouched.</done>
</task>

</tasks>

<verification_criteria>
Plan 04 succeeds when:

- `docs/60_SECURITY_BASELINE.md` exists with 6 H2 sections covering the 6 ejes, the audit log model table, retention wording, CCFE Fernet blueprint with NO-execute marker, D-09 "validar en Pre-Fase 3" markers on snippets, cross-refs to docs/61 and restore-smoke.sh
- `scripts/restore-smoke.sh` exists as executable stub, parses syntactically, exits 0
- `README.md` has exactly 1 new badge + 1 new "Seguridad" section (no other changes)
- `pre-commit run --all-files` green
- All 3 security workflow jobs from Plan 02 stay green on the PR for this plan
- No regression to "SENAC" or "gdpr_purge" anywhere in docs/60
  </verification_criteria>

<success_criteria>

- SEC-06 closed: 6 ejes documented operationally; CCFE blueprint dense enough for Fase 2 EDI; backup strategy stubbed with restore-smoke.sh
- README badge gives visible signal that security workflow is active; "Seguridad" section directs reporters to SECURITY.md
- Plan-04 ships in parallel with Plan-05 (both Wave 4, no file overlap) — they can be reviewed independently
  </success_criteria>

<output>
Create `.planning/phases/02-bloque-b-security-baseline/02-04-SUMMARY.md` when done, including: line count of docs/60, list of the 6 H2 section titles as authored, confirmation restore-smoke.sh is executable in git, the line range of README modifications (before/after).
</output>
