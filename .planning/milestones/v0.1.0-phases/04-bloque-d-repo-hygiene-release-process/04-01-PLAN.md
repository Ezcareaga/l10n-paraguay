---
phase: 04-bloque-d-repo-hygiene-release-process
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/ISSUE_TEMPLATE/bug_report.yml
  - .github/ISSUE_TEMPLATE/feature_request.yml
  - .github/ISSUE_TEMPLATE/config.yml
autonomous: true
requirements: [REL-01]
must_haves:
  truths:
    - "Opening a new issue presents a chooser with exactly 2 forms (Bug Report, Feature Request)"
    - "Blank free-form issues are blocked (blank_issues_enabled: false)"
    - "Questions are routed to GitHub Discussions, security reports to the private security policy — neither is a public issue"
  artifacts:
    - path: ".github/ISSUE_TEMPLATE/bug_report.yml"
      provides: "Structured bug intake form, auto-labels 'bug'"
      contains: "name: Bug Report"
    - path: ".github/ISSUE_TEMPLATE/feature_request.yml"
      provides: "Structured feature intake form, auto-labels 'enhancement'"
      contains: "name: Feature Request"
    - path: ".github/ISSUE_TEMPLATE/config.yml"
      provides: "blank_issues_enabled:false + Discussions + security contact_links"
      contains: "blank_issues_enabled: false"
  key_links:
    - from: ".github/ISSUE_TEMPLATE/config.yml"
      to: "https://github.com/Ezcareaga/l10n-paraguay/security/policy"
      via: "contact_links security entry"
      pattern: "security/policy"
    - from: ".github/ISSUE_TEMPLATE/config.yml"
      to: "https://github.com/Ezcareaga/l10n-paraguay/discussions"
      via: "contact_links Q&A entry"
      pattern: "discussions"
---

<objective>
Create the GitHub issue intake layer: two structured YAML issue forms
(`bug_report.yml`, `feature_request.yml`) plus `config.yml` that disables blank
issues and routes questions to Discussions and security reports to the private
security policy.

Purpose: Give contributors a frictionless, structured way to file actionable
issues while keeping questions and security reports off the public issue tracker
(REL-01 / D-02). This is the contributor-facing front door of the repo.

Output: 3 new files under `.github/ISSUE_TEMPLATE/`. Per Amendment A-01,
`question.yml` is DELIBERATELY OMITTED — questions go to Discussions instead.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-bloque-d-repo-hygiene-release-process/04-CONTEXT.md
@.planning/phases/04-bloque-d-repo-hygiene-release-process/04-RESEARCH.md
@.planning/phases/04-bloque-d-repo-hygiene-release-process/04-VALIDATION.md

<notes>
- All `.github/*` meta files MUST be in ENGLISH (D-01 Phase 3 carry-forward).
- `04-RESEARCH.md` Pattern 1 (issue forms) + Pattern 2 (config.yml) contain
  VERIFIED, ready-to-use skeletons — copy from there, do not re-invent schema.
- `.yamllint` (repo root) already sets `truthy: check-keys: false`,
  `line-length: warning`, `document-start: disable` — issue-form YAML keys
  (`value:`, `required:`) will NOT trip the linter. If yamllint still fails on
  the ISSUE_TEMPLATE files, add `ignore: |\n  .github/ISSUE_TEMPLATE/` to
  `.yamllint` (RESEARCH Assumption A4 / Open Question 2 fallback).
- Discussions is NOT enabled yet at this plan's execution time (Plan 03 enables
  it). Use the GENERIC `/discussions` URL for the Q&A contact_link now
  (RESEARCH Pitfall 4 mitigation); Plan 03 may refine it to the Q&A category slug.
- `question.yml` is intentionally absent — do NOT create it (A-01).
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bug_report.yml and feature_request.yml issue forms</name>
  <files>.github/ISSUE_TEMPLATE/bug_report.yml, .github/ISSUE_TEMPLATE/feature_request.yml</files>
  <read_first>
    - .planning/phases/04-bloque-d-repo-hygiene-release-process/04-RESEARCH.md (Pattern 1 — verified bug_report.yml + feature_request.yml skeletons; Pitfall 1 on label pre-existence)
    - .planning/phases/04-bloque-d-repo-hygiene-release-process/04-CONTEXT.md (D-02 + Amendment A-01)
    - .yamllint (confirm form keys won't fail lint)
  </read_first>
  <action>
    Create `.github/ISSUE_TEMPLATE/bug_report.yml` from the RESEARCH Pattern 1
    bug_report skeleton: top-level `name: Bug Report`, `description`,
    `title: "[Bug]: "`, `labels: ["bug"]`; body with markdown intro + required
    textareas (`description` "What happened?", `expected`, `steps`), required
    `input` `odoo_version` (placeholder "18.0"), optional `textarea` `logs`
    (`render: shell`), and a required `checkboxes` block (searched existing
    issues; using Odoo Community 18.0 not Enterprise).
    Create `.github/ISSUE_TEMPLATE/feature_request.yml` from the RESEARCH
    feature_request skeleton: `name: Feature Request`, `title: "[Feature]: "`,
    `labels: ["enhancement"]`; body with markdown intro + required textareas
    (`problem`, `solution`), optional `alternatives`, required `checkboxes`
    (searched existing issues and discussions).
    Both files in ENGLISH. Use `["bug"]` / `["enhancement"]` (GitHub default
    labels — already exist, so auto-labeling works immediately). Do NOT create
    `question.yml` (A-01).
  </action>
  <verify>
    <automated>test -f .github/ISSUE_TEMPLATE/bug_report.yml && test -f .github/ISSUE_TEMPLATE/feature_request.yml && grep -q 'name: Bug Report' .github/ISSUE_TEMPLATE/bug_report.yml && grep -q 'name: Feature Request' .github/ISSUE_TEMPLATE/feature_request.yml && grep -q 'labels: \["bug"\]' .github/ISSUE_TEMPLATE/bug_report.yml && pre-commit run yamllint --files .github/ISSUE_TEMPLATE/bug_report.yml .github/ISSUE_TEMPLATE/feature_request.yml</automated>
  </verify>
  <acceptance_criteria>
    - Both form files exist and parse as valid YAML (yamllint green).
    - `bug_report.yml` declares `labels: ["bug"]`; `feature_request.yml` declares `labels: ["enhancement"]`.
    - No `question.yml` file exists in `.github/ISSUE_TEMPLATE/`.
    - All content is in English.
  </acceptance_criteria>
  <done>Two issue-form YAML files exist, lint-clean, with the required fields and default-label auto-tagging.</done>
</task>

<task type="auto">
  <name>Task 2: Create config.yml routing questions to Discussions and security to private policy</name>
  <files>.github/ISSUE_TEMPLATE/config.yml</files>
  <read_first>
    - .planning/phases/04-bloque-d-repo-hygiene-release-process/04-RESEARCH.md (Pattern 2 — verified config.yml; URL notes; Pitfall 3 HTTP/HTTPS requirement; Pitfall 4 Discussions-URL fallback)
    - .planning/phases/04-bloque-d-repo-hygiene-release-process/04-CONTEXT.md (D-02 — Discussions + SECURITY.md routing)
    - SECURITY.md (the private security channel the security contact_link surfaces)
  </read_first>
  <action>
    Create `.github/ISSUE_TEMPLATE/config.yml` with `blank_issues_enabled: false`
    and a `contact_links` array of exactly two entries:
    (1) name "Q&A — GitHub Discussions", url
    `https://github.com/Ezcareaga/l10n-paraguay/discussions` (GENERIC discussions
    URL — Discussions not yet enabled; Plan 03 may refine to the Q&A category
    slug), about text directing questions to a Discussion instead of an issue;
    (2) name "Report a Security Vulnerability", url
    `https://github.com/Ezcareaga/l10n-paraguay/security/policy` (GitHub's
    canonical SECURITY.md render URL — must be full HTTPS, NOT a relative file
    path per Pitfall 3), about text instructing users to use the private channel
    and NOT open a public issue. English only.
  </action>
  <verify>
    <automated>test -f .github/ISSUE_TEMPLATE/config.yml && grep -q 'blank_issues_enabled: false' .github/ISSUE_TEMPLATE/config.yml && grep -q 'security/policy' .github/ISSUE_TEMPLATE/config.yml && grep -q 'discussions' .github/ISSUE_TEMPLATE/config.yml && pre-commit run yamllint --files .github/ISSUE_TEMPLATE/config.yml</automated>
  </verify>
  <acceptance_criteria>
    - `config.yml` exists, yamllint green, `blank_issues_enabled: false` present.
    - Exactly two `contact_links`: one Discussions URL, one `.../security/policy` URL.
    - Both URLs are full `https://...` (no relative file paths).
  </acceptance_criteria>
  <done>config.yml blocks blank issues and surfaces Discussions + private security policy as the two non-form intake routes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                                 |
| -------------------------------------------- | ----------------------------------------------------------- |
| anonymous contributor → GitHub issue tracker | Untrusted public input enters via the issue chooser / forms |

## STRIDE Threat Register

| Threat ID | Category               | Component                          | Disposition | Mitigation Plan                                                                                                                                                                                                                                       |
| --------- | ---------------------- | ---------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-01   | Information Disclosure | `config.yml` security contact_link | mitigate    | `blank_issues_enabled: false` + security contact_link → `/security/policy` routes vulnerability reports to the private channel instead of a public issue; verify the URL is the canonical `/security/policy` render of SECURITY.md                    |
| T-04-02   | Tampering              | issue-form `labels:` field         | accept      | GitHub only auto-applies labels that already exist in the repo; `bug`/`enhancement` are defaults, so no arbitrary label injection is possible via forms. Low risk, no PII surface                                                                     |
| T-04-03   | Information Disclosure | Q&A contact_link URL               | mitigate    | Use the generic `/discussions` URL while Discussions is not yet enabled (Pitfall 4) to avoid a dead 404 link that could push a frustrated user to file a public issue with sensitive content; Plan 03 refines to the Q&A category slug after enabling |

No HIGH-severity threats. This plan ships no auth/secret-handling code; threats are routing/disclosure only.
</threat_model>

<verification>
- `pre-commit run --all-files` green (no yamllint regression from new `.github/ISSUE_TEMPLATE/*.yml`).
- `ls .github/ISSUE_TEMPLATE/` lists exactly `bug_report.yml`, `feature_request.yml`, `config.yml` (NO `question.yml`).
- Post-merge GitHub-UI smoke (manual, after Plan 03 enables Discussions): open
  `https://github.com/Ezcareaga/l10n-paraguay/issues/new/choose` → 2 forms + 2
  contact links + no blank-issue option.
</verification>

<success_criteria>
REL-01 satisfied (under Amendment A-01): `.github/ISSUE_TEMPLATE/` contains
`bug_report.yml` + `feature_request.yml` + `config.yml` with Discussions +
security links; blank issues disabled; `question.yml` intentionally absent.
</success_criteria>

<output>
Create `.planning/phases/04-bloque-d-repo-hygiene-release-process/04-01-SUMMARY.md` when done.
</output>
