---
phase: 04-bloque-d-repo-hygiene-release-process
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/CODEOWNERS
  - .github/release.yml
  - .github/PULL_REQUEST_TEMPLATE.md
  - CONTRIBUTING.md
autonomous: true
requirements: [REL-02, REL-03, REL-04, REL-06]
must_haves:
  truths:
    - "Every PR opened to main auto-requests review from @Ezcareaga (global CODEOWNER)"
    - "Opening a PR via the web UI auto-populates a checklist reminder body"
    - "release.yml groups release notes by PR label into Keep-a-Changelog categories and excludes bot PRs"
    - "CONTRIBUTING.md documents the manual 4-step release process — no placeholder remains"
  artifacts:
    - path: ".github/CODEOWNERS"
      provides: "Global owner @Ezcareaga + commented area stubs"
      contains: "* @Ezcareaga"
    - path: ".github/release.yml"
      provides: "label→category map + bot/skip-changelog exclusion"
      contains: "changelog:"
    - path: ".github/PULL_REQUEST_TEMPLATE.md"
      provides: "Soft-reminder PR checklist (tests, pre-commit, ADR, Conventional Commits)"
      contains: "## Checklist"
    - path: "CONTRIBUTING.md"
      provides: "Manual release process — 4 ordered steps + label→category map"
      contains: "## Release process"
  key_links:
    - from: ".github/release.yml"
      to: "dependabot[bot]"
      via: "changelog.exclude.authors"
      pattern: "dependabot\\[bot\\]"
    - from: "CONTRIBUTING.md §Release process"
      to: ".github/release.yml"
      via: "documented label→category map"
      pattern: "release notes category"
---

<objective>
Create the PR-hygiene and release-categorization layer: `CODEOWNERS`
(global `@Ezcareaga` + commented stubs), `release.yml` (label→category map),
`PULL_REQUEST_TEMPLATE.md` (soft-reminder checklist), and replace the
CONTRIBUTING.md "## Release process" placeholder with the documented manual
4-step process.

Purpose: Route reviews to the maintainer, give contributors a self-service PR
checklist, auto-categorize release notes by PR label, and write down the
manual-release decision + steps (REL-02, REL-03, REL-04, REL-06 / D-01, D-03, D-04).

Output: 3 new `.github/` files + 1 edited CONTRIBUTING.md section. This is the
final local-file content for the v0.1.0 rollback point; Plan 03 creates the
labels these files reference, Plan 04 publishes the release.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-bloque-d-repo-hygiene-release-process/04-CONTEXT.md
@.planning/phases/04-bloque-d-repo-hygiene-release-process/04-RESEARCH.md
@.planning/phases/04-bloque-d-repo-hygiene-release-process/04-VALIDATION.md

<notes>
- All `.github/*` meta files + the CONTRIBUTING release section MUST be in
  ENGLISH (D-01 Phase 3 carry-forward).
- `04-RESEARCH.md` Pattern 3 (release.yml — verified full skeleton + label
  inventory), Pattern 4 (CODEOWNERS — verified), Pattern 5 (PR template —
  verified), Pattern 8 (CONTRIBUTING release section — verified replacement
  text + label→category table) contain ready-to-use content — copy from there.
- CODEOWNERS rule order: `* @Ezcareaga` FIRST (last-match-wins; RESEARCH Pitfall 5).
- `release.yml` is read only from the default branch (`main`) at release time —
  it must be merged before the v0.1.0 tag (RESEARCH Pitfall 2; satisfied by wave
  order, this is Wave 1).
- The CONTRIBUTING placeholder to replace is the block at lines ~216-220:
  the `## Release process` heading + the `> **Deferred to Phase 4 (REL-06).**`
  blockquote. Replace the blockquote with the manual process; KEEP the
  `## Release process` heading and the surrounding `---` separators intact.
- The release.yml label categories reference labels that do NOT yet exist
  (`feat`, `fix`, `changed`, `security`, `docs`, `skip-changelog`, etc.) —
  that is expected; Plan 03 creates them. The file is valid YAML regardless.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CODEOWNERS and PULL_REQUEST_TEMPLATE.md</name>
  <files>.github/CODEOWNERS, .github/PULL_REQUEST_TEMPLATE.md</files>
  <read_first>
    - .planning/phases/04-bloque-d-repo-hygiene-release-process/04-RESEARCH.md (Pattern 4 CODEOWNERS + Pitfall 5 ordering; Pattern 5 PR template)
    - .planning/phases/04-bloque-d-repo-hygiene-release-process/04-CONTEXT.md (D-04 — global owner + commented stubs; checklist as soft reminders)
  </read_first>
  <action>
    Create `.github/CODEOWNERS` from RESEARCH Pattern 4: first non-comment line
    `* @Ezcareaga` (global owner), followed by an "Uncomment area stubs as
    contributors join:" comment and commented-out area rules
    (`# /addons/l10n_py_base/   @Ezcareaga`, `# /addons/l10n_py_account/`,
    `# /addons/l10n_py_edi/`, `# /docs/`, `# /.github/`). Global rule MUST be
    first (last-match-wins).
    Create `.github/PULL_REQUEST_TEMPLATE.md` from RESEARCH Pattern 5: a
    `## Checklist` of soft-reminder items (tests pass `pytest addons/ -x`;
    pre-commit passes; Conventional Commit format; docs updated if behavior
    changed; new ADR in same PR if architectural — DOC-09; CHANGELOG updated if
    release-worthy), a `## Description` section with an HTML comment prompt
    (link issues with "Closes #N"), and a `## Type of change` checkbox list
    (fix / feat / docs / refactor / ci-build / other). Soft reminders only — NOT
    hard gates (branch protection already enforces CI). English only.
  </action>
  <verify>
    <automated>test -f .github/CODEOWNERS && test -f .github/PULL_REQUEST_TEMPLATE.md && grep -q '^\* @Ezcareaga' .github/CODEOWNERS && head -n 1 .github/CODEOWNERS | grep -qv '^#' && grep -q '## Checklist' .github/PULL_REQUEST_TEMPLATE.md && grep -q 'ADR' .github/PULL_REQUEST_TEMPLATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^\* @Ezcareaga' .github/CODEOWNERS` succeeds and that rule is the FIRST non-comment line.
    - Area stubs are present but commented (`#`), having zero effect until uncommented.
    - PR template contains a `## Checklist` with the ADR/DOC-09 reminder and a `## Type of change` section.
  </acceptance_criteria>
  <done>CODEOWNERS routes all PRs to @Ezcareaga with inert future-area stubs; PR template auto-populates a soft checklist.</done>
</task>

<task type="auto">
  <name>Task 2: Create release.yml and replace the CONTRIBUTING.md release placeholder</name>
  <files>.github/release.yml, CONTRIBUTING.md</files>
  <read_first>
    - .planning/phases/04-bloque-d-repo-hygiene-release-process/04-RESEARCH.md (Pattern 3 release.yml verified skeleton + label inventory; Pattern 8 CONTRIBUTING replacement text + label→category table)
    - .planning/phases/04-bloque-d-repo-hygiene-release-process/04-CONTEXT.md (D-01 manual release 4 steps; D-03 label-based categories)
    - CONTRIBUTING.md (read lines ~210-225 to see the exact placeholder block + surrounding `---` separators)
  </read_first>
  <action>
    Create `.github/release.yml` from RESEARCH Pattern 3: top-level `changelog:`
    with `exclude` (labels: `skip-changelog`; authors: `dependabot[bot]`) and
    `categories` in this order — "Added" (`feat`, `enhancement`), "Fixed"
    (`bug`, `fix`), "Changed" (`changed`, `refactor`, `chore`), "Security"
    (`security`), "Documentation" (`documentation`, `docs`), "Dependencies"
    (`dependencies`), "Other" (`"*"` catch-all last).
    In `CONTRIBUTING.md`, REPLACE the blockquote placeholder under `## Release
    process` (the `> **Deferred to Phase 4 (REL-06).** ...` block, currently
    lines ~218-220) with the RESEARCH Pattern 8 content: a "Decision: manual
    releases" rationale paragraph (single-maintainer, early history,
    reassess later), a numbered "Steps (for maintainer @Ezcareaga)" list of the
    4 manual steps (compile CHANGELOG entry → merge to main with 6 checks green
    → `git tag -a vX.Y.Z` + push → `gh release create ... --notes-file ...
    --latest`), and the "PR label → release notes category" mapping table.
    KEEP the `## Release process` heading and the `---` separators around the
    section. English only.
  </action>
  <verify>
    <automated>test -f .github/release.yml && grep -q 'changelog:' .github/release.yml && grep -q 'dependabot\[bot\]' .github/release.yml && grep -q 'skip-changelog' .github/release.yml && grep -q '"\*"' .github/release.yml && pre-commit run yamllint --files .github/release.yml && grep -q '## Release process' CONTRIBUTING.md && ! grep -q 'Deferred to Phase 4' CONTRIBUTING.md && grep -qi 'git tag' CONTRIBUTING.md</automated>
  </verify>
  <acceptance_criteria>
    - `release.yml` is valid YAML (yamllint green), excludes `dependabot[bot]` + `skip-changelog`, and ends with the `"*"` catch-all category.
    - `grep 'Deferred to Phase 4' CONTRIBUTING.md` returns NOTHING (placeholder gone).
    - CONTRIBUTING `## Release process` now contains the 4 manual steps (incl. a `git tag` command) and the label→category table.
  </acceptance_criteria>
  <done>release.yml categorizes notes by label and excludes bots; CONTRIBUTING documents the manual release process with no placeholder remaining.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                            |
| ----------------------------------- | ------------------------------------------------------ |
| contributor PR → main (review gate) | CODEOWNERS + branch protection decide who must approve |
| merged PR → public release notes    | release.yml decides what appears in published notes    |

## STRIDE Threat Register

| Threat ID | Category                | Component                            | Disposition | Mitigation Plan                                                                                                                                                                                                                                  |
| --------- | ----------------------- | ------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-04-04   | Repudiation / Elevation | `.github/CODEOWNERS`                 | mitigate    | A typo'd handle would silently disable required CODEOWNER review. Verify `* @Ezcareaga` is the first non-comment line and that `@Ezcareaga` resolves (real GitHub owner); branch protection (existing, 6 checks) still requires CODEOWNER review |
| T-04-05   | Information Disclosure  | `.github/release.yml`                | mitigate    | Automated/bot PRs leaking into public release notes is avoided via `exclude.authors: [dependabot[bot]]` + `exclude.labels: [skip-changelog]`; `"*"` catch-all ensures no labeled PR is silently dropped                                          |
| T-04-06   | Tampering               | release.yml read from default branch | accept      | GitHub reads `release.yml` only from `main` at release time; it must be merged before tagging — guaranteed by wave order (this is Wave 1, tag is Wave 3). Low risk                                                                               |

No HIGH-severity threats. This plan ships no executable code; threats are review-routing and release-notes hygiene.
</threat_model>

<verification>
- `pre-commit run --all-files` green (release.yml + CONTRIBUTING edit lint-clean).
- `grep -n 'Deferred to Phase 4' CONTRIBUTING.md` returns nothing.
- `grep -c 'title:' .github/release.yml` shows the 7 categories.
- Post-merge GitHub-UI smoke (manual): open a test PR via web UI → PR template
  body auto-populates; `@Ezcareaga` auto-requested as reviewer.
</verification>

<success_criteria>
REL-02 (PR template), REL-03 (CODEOWNERS global + stubs), REL-04 (release.yml
label→category map + bot exclusion), and REL-06 (manual release process
documented in CONTRIBUTING, placeholder removed) all satisfied.
</success_criteria>

<output>
Create `.planning/phases/04-bloque-d-repo-hygiene-release-process/04-02-SUMMARY.md` when done.
</output>
