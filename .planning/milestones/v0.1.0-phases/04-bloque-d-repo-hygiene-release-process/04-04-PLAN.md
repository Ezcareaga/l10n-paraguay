---
phase: 04-bloque-d-repo-hygiene-release-process
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - CHANGELOG.md
autonomous: false
requirements: [REL-05]
must_haves:
  truths:
    - "CHANGELOG.md [0.1.0] entry is date-stamped (no longer '- Unreleased')"
    - "Tag v0.1.0 exists on main and points to the merge commit with all Phase 4 work"
    - "GitHub Release v0.1.0 is published as a full release (latest, not draft, not pre-release) with CHANGELOG-sourced notes"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Date-stamped [0.1.0] release entry"
      contains: "## [0.1.0] -"
    - path: "(GitHub Release v0.1.0)"
      provides: "Verifiable rollback point post-Fase 1 + foundation"
  key_links:
    - from: "git tag v0.1.0"
      to: "main HEAD (Phase 4 merge commit)"
      via: "annotated tag on main"
      pattern: "v0.1.0"
    - from: "GitHub Release v0.1.0"
      to: "CHANGELOG.md [0.1.0] section"
      via: "--notes-file extracted CHANGELOG section"
      pattern: "notes-file"
---

<objective>
Date-stamp the CHANGELOG `[0.1.0]` entry, tag `v0.1.0` on `main`, and publish the
GitHub Release as a full (latest) release with notes sourced from the CHANGELOG.
This is the LAST step of the phase — it depends on Plans 04-01, 04-02 (all
`.github/` files + CONTRIBUTING edit merged) and 04-03 (Discussions + labels) so
the tagged state is complete and `release.yml` is already on `main`.

Purpose: Create the first verifiable rollback point for the project — the
post-Fase 1 + Pre-Fase 2 foundation snapshot (REL-05 / D-05). It is a milestone
DoD item, not decorative.

Output: Date-stamped CHANGELOG, `v0.1.0` annotated tag on `main`, published
GitHub Release v0.1.0 (latest). Outward-facing — maintainer runs the git/gh
commands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-bloque-d-repo-hygiene-release-process/04-CONTEXT.md
@.planning/phases/04-bloque-d-repo-hygiene-release-process/04-RESEARCH.md
@.planning/phases/04-bloque-d-repo-hygiene-release-process/04-VALIDATION.md
@CHANGELOG.md

<notes>
- This plan MUST run only after Plans 04-01, 04-02, 04-03 are complete and the
  `.github/` files + CONTRIBUTING edit are MERGED to `main` (RESEARCH Pitfall 2
  + Pitfall 6: release.yml must be on main; tag must point to a complete main).
- The CHANGELOG `[0.1.0]` entry already has rich content (the 2 modules + CI/CD +
  security + docs foundation). This plan ONLY changes the heading date and
  publishes — do NOT rewrite the entry body.
- Current heading (CHANGELOG.md line 9):
  `## [0.1.0] - Unreleased (see Phase 4 REL-05 for tag date)`
  Replace with: `## [0.1.0] - YYYY-MM-DD` (actual tag date).
- D-05: full release (`--latest`), NOT `--draft`, NOT `--prerelease`. Manual
  notes via `--notes-file` (richer than `--generate-notes` for a foundation
  release with no prior tag — RESEARCH Pattern 6).
- RESEARCH Pattern 6 has the exact `git tag -a` + `gh release create` commands.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Date-stamp the CHANGELOG [0.1.0] entry</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (see line 9 — the `[0.1.0] - Unreleased` heading to date-stamp; do not touch the entry body)
    - .planning/phases/04-bloque-d-repo-hygiene-release-process/04-RESEARCH.md (Pattern 6 Step 2 — date-stamp)
    - .planning/phases/04-bloque-d-repo-hygiene-release-process/04-CONTEXT.md (D-05)
  </read_first>
  <action>
    In `CHANGELOG.md`, replace the line
    `## [0.1.0] - Unreleased (see Phase 4 REL-05 for tag date)`
    with `## [0.1.0] - <today's date YYYY-MM-DD>` (the actual tag date). Leave the
    `## [Unreleased]` section above it and the entire `[0.1.0]` entry body
    (Added / Changed) UNCHANGED. Leave the `[0.1.0]:` link reference at the
    bottom unchanged.
  </action>
  <verify>
    <automated>! grep -q '\[0.1.0\] - Unreleased' CHANGELOG.md && grep -qE '## \[0.1.0\] - [0-9]{4}-[0-9]{2}-[0-9]{2}' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep '\[0.1.0\] - Unreleased' CHANGELOG.md` returns NOTHING.
    - `## [0.1.0] - YYYY-MM-DD` (an ISO date) is present.
    - The `[0.1.0]` entry body is byte-identical except the heading line.
  </acceptance_criteria>
  <done>CHANGELOG [0.1.0] is date-stamped, ready to source the release notes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Merge date-stamp, tag v0.1.0, and publish the GitHub Release (maintainer action)</name>
  <read_first>
    - .planning/phases/04-bloque-d-repo-hygiene-release-process/04-RESEARCH.md (Pattern 6 — full command sequence; Pitfall 6 tag-on-main verification)
    - .planning/phases/04-bloque-d-repo-hygiene-release-process/04-CONTEXT.md (D-05 — full release, latest, manual notes)
    - CHANGELOG.md (source of the [0.1.0] release notes)
  </read_first>
  <what-built>
    Task 1 date-stamped the CHANGELOG `[0.1.0]` entry. All `.github/` files +
    CONTRIBUTING edit (Plans 04-01/02) and Discussions + labels (Plan 04-03) are
    merged/done. This step ships the date-stamp to `main`, tags the release
    commit, and publishes the GitHub Release.
  </what-built>
  <action>
    Maintainer runs, in order (RESEARCH Pattern 6):
    1. Commit + merge the date-stamp to `main` via PR (6 checks green), then:
       ```
       git checkout main && git pull origin main
       ```
    2. VERIFY the tag will point to complete main (Pitfall 6):
       confirm `git log --oneline -1` is the Phase 4 merge commit (all REL-01..06 work present).
    3. Create + push the annotated tag:
       ```
       git tag -a v0.1.0 -m "Release v0.1.0 — foundation milestone (l10n_py_base + l10n_py_account + Pre-Fase 2)"
       git push origin v0.1.0
       ```
    4. Extract the `## [0.1.0]` section from CHANGELOG.md into a temp file
       `CHANGELOG_SECTION.md` (the lines between `## [0.1.0]` and the next `##`
       heading / the link-reference block).
    5. Publish the full release (NOT draft, NOT prerelease):
       ```
       gh release create v0.1.0 --title "v0.1.0 — Foundation milestone" --notes-file CHANGELOG_SECTION.md --latest
       ```
    6. Delete the temp `CHANGELOG_SECTION.md` after publishing.
  </action>
  <how-to-verify>
    1. `gh release view v0.1.0` shows the release with the CHANGELOG-sourced notes.
    2. `gh release view v0.1.0 --json isLatest,isDraft,isPrerelease` →
       `isLatest:true, isDraft:false, isPrerelease:false`.
    3. `git log v0.1.0 --oneline -1` matches `git log main --oneline -1` (tag on the merge commit).
  </how-to-verify>
  <verify>
    <automated>gh release view v0.1.0 --json isLatest,isDraft,isPrerelease --jq '.isLatest and (.isDraft|not) and (.isPrerelease|not)' | grep -q true</automated>
  </verify>
  <acceptance_criteria>
    - `gh release view v0.1.0` succeeds and shows manual notes (CHANGELOG `[0.1.0]` content).
    - Release flags: latest=true, draft=false, prerelease=false.
    - `v0.1.0` tag points to the `main` commit containing all Phase 4 work.
  </acceptance_criteria>
  <resume-signal>Type "v0.1.0 published" once `gh release view v0.1.0` shows the full latest release.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                        |
| ---------------------------------------------- | -------------------------------------------------- |
| maintainer git/gh → published release artifact | The tag + release become the public rollback point |

## STRIDE Threat Register

| Threat ID | Category               | Component             | Disposition | Mitigation Plan                                                                                                                                                                                                               |
| --------- | ---------------------- | --------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-09   | Integrity              | `v0.1.0` tag target   | mitigate    | Tagging a non-`main` or incomplete commit would make the rollback point misleading (Pitfall 6); the maintainer verifies `git log v0.1.0 --oneline -1` == `git log main --oneline -1` (Phase 4 merge commit) before publishing |
| T-04-10   | Information Disclosure | release notes content | accept      | Notes are sourced from the curated CHANGELOG `[0.1.0]` (no auto PR dump), so no bot noise or unintended content; manual `--notes-file`, not `--generate-notes`. Low risk                                                      |

No HIGH-severity threats. No code or secrets shipped; the artifact is a tag + notes.
</threat_model>

<verification>
- `! grep -q '\[0.1.0\] - Unreleased' CHANGELOG.md` (date-stamped).
- `gh release view v0.1.0` returns a full, latest, non-draft, non-prerelease release.
- `git log v0.1.0 --oneline -1` == `git log main --oneline -1`.
</verification>

<success_criteria>
REL-05 satisfied: `v0.1.0` published on GitHub Releases as a full (latest)
release with manual CHANGELOG-sourced notes describing the post-Fase 1 +
foundation snapshot; CHANGELOG `[0.1.0]` date-stamped. First verifiable rollback
point for the project exists.
</success_criteria>

<output>
Create `.planning/phases/04-bloque-d-repo-hygiene-release-process/04-04-SUMMARY.md` when done.
</output>
