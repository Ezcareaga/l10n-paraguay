---
phase: 03-bloque-c-documentaci-n-operacional
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/65_FASE_1_RETROSPECTIVA.md
  - CLAUDE.md
  - AGENTS.md
  - .planning/PROJECT.md
  - .pre-commit-config.yaml
  - addons/l10n_py_base/README.rst
  - addons/l10n_py_account/README.rst
autonomous: true
requirements: [DOC-08]
must_haves:
  truths:
    - "docs/60 prefix is free for SECURITY_BASELINE; retrospectiva lives at docs/65"
    - "No file in the repo links to the old docs/60_FASE_1_RETROSPECTIVA.md path"
    - "oca-gen-addon-readme hook is active and addon README.rst files are regenerated from fragments"
  artifacts:
    - path: "docs/65_FASE_1_RETROSPECTIVA.md"
      provides: "Renamed retrospectiva (was docs/60_FASE_1_RETROSPECTIVA.md)"
    - path: ".pre-commit-config.yaml"
      provides: "oca-gen-addon-readme hook added to maintainer-tools block"
      contains: "oca-gen-addon-readme"
  key_links:
    - from: "CLAUDE.md"
      to: "docs/65_FASE_1_RETROSPECTIVA.md"
      via: "markdown link"
      pattern: "65_FASE_1_RETROSPECTIVA"
---

<objective>
Foundation prep for Phase 3 docs: (1) resolve the docs/60 prefix collision by
renaming docs/60_FASE_1_RETROSPECTIVA.md → docs/65_FASE_1_RETROSPECTIVA.md and
fixing every link (D-05), and (2) activate the oca-gen-addon-readme pre-commit
hook, absorbing the regenerated addon README.rst diff in a dedicated commit (D-07).

Purpose: Both are ordering-critical. The rename MUST land before any later plan
writes a doc that could link to the retrospectiva (Pitfall 2). The hook activation
produces a cosmetic README.rst regeneration diff that must be isolated in its own
commit so `git blame` stays clean (D-07, Phase 1 baseline pattern).

Output: docs/65_FASE_1_RETROSPECTIVA.md (renamed), updated links in CLAUDE.md +
AGENTS.md + .planning/PROJECT.md, .pre-commit-config.yaml with the new hook,
regenerated addons/\*/README.rst.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bloque-c-documentaci-n-operacional/03-CONTEXT.md
@.planning/phases/03-bloque-c-documentaci-n-operacional/03-RESEARCH.md
@.planning/phases/03-bloque-c-documentaci-n-operacional/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename retrospectiva docs/60 → docs/65 and fix all links (D-05)</name>
  <files>docs/65_FASE_1_RETROSPECTIVA.md, CLAUDE.md, AGENTS.md, .planning/PROJECT.md</files>
  <read_first>
    - docs/60_FASE_1_RETROSPECTIVA.md (the file being renamed — confirm content; content is unchanged, only the path changes)
    - CLAUDE.md (line ~122 references docs/60_FASE_1_RETROSPECTIVA.md)
    - AGENTS.md (references docs/60_FASE_1_RETROSPECTIVA.md)
    - .planning/PROJECT.md (references 60_FASE_1)
    - .planning/phases/03-bloque-c-documentaci-n-operacional/03-RESEARCH.md (Runtime State Inventory — the 4 link locations)
  </read_first>
  <action>
    Run `git mv docs/60_FASE_1_RETROSPECTIVA.md docs/65_FASE_1_RETROSPECTIVA.md` so the
    rename is tracked. The `65_` prefix is confirmed free (only 60 and 61 exist in the
    docs/6x range; 62-69 are open) and frees prefix `60` for the existing
    docs/60_SECURITY_BASELINE.md.
    Then update every occurrence of the string `60_FASE_1_RETROSPECTIVA` to
    `65_FASE_1_RETROSPECTIVA` in the three non-planning files that link to it:
    CLAUDE.md, AGENTS.md, .planning/PROJECT.md. Do NOT edit files under
    .planning/phases/ (historical planning artifacts — read-only; updating them is
    optional and out of scope). The file body of the renamed doc itself is unchanged.
    Use a Conventional Commit: `chore(docs): rename 60_FASE_1_RETROSPECTIVA to 65_ (D-05)`.
  </action>
  <verify>
    <automated>test -f docs/65_FASE_1_RETROSPECTIVA.md && ! test -f docs/60_FASE_1_RETROSPECTIVA.md && grep -rL "60_FASE_1_RETROSPECTIVA" CLAUDE.md AGENTS.md .planning/PROJECT.md >/dev/null && ! grep -l "60_FASE_1_RETROSPECTIVA" CLAUDE.md AGENTS.md .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/65_FASE_1_RETROSPECTIVA.md` exists; `docs/60_FASE_1_RETROSPECTIVA.md` does not
    - `git status` shows the change as a rename (R), not delete+add
    - `grep "60_FASE_1_RETROSPECTIVA" CLAUDE.md AGENTS.md .planning/PROJECT.md` returns no matches
    - `grep -c "65_FASE_1_RETROSPECTIVA" CLAUDE.md` returns ≥1
    - `docs/60_SECURITY_BASELINE.md` still exists and is untouched (prefix 60 now unambiguous)
  </acceptance_criteria>
  <done>Retrospectiva renamed to docs/65, all three non-planning links updated, no broken references to the old path remain.</done>
</task>

<task type="auto">
  <name>Task 2: Activate oca-gen-addon-readme hook + regenerate addon READMEs (D-07)</name>
  <files>.pre-commit-config.yaml, addons/l10n_py_base/README.rst, addons/l10n_py_account/README.rst</files>
  <read_first>
    - .pre-commit-config.yaml (lines 36-41: the maintainer-tools repo block pinned at rev b89f767503be6ab2b11e4f50a7557cb20066e667; lines 9-20 global exclude block; lines 132-140 trailing-whitespace/end-of-file-fixer/check-merge-conflict README.rst excludes)
    - addons/l10n_py_base/readme/ (fragment tree: DESCRIPTION.rst, USAGE.rst, CONTRIBUTORS.rst, CREDITS.rst, CHANGES.rst)
    - addons/l10n_py_account/readme/ (fragment tree: DESCRIPTION.rst, USAGE.rst, CONFIGURE.rst, ROADMAP.rst, CHANGES.rst)
    - .planning/phases/03-bloque-c-documentaci-n-operacional/03-RESEARCH.md (Pitfall 1: exclude collision; Code Examples: oca-gen-addon-readme entry)
    - .planning/phases/03-bloque-c-documentaci-n-operacional/03-PATTERNS.md (.pre-commit-config.yaml section: exact block to modify)
  </read_first>
  <action>
    Add hook id `oca-gen-addon-readme` to the EXISTING `https://github.com/oca/maintainer-tools`
    repo block in .pre-commit-config.yaml (rev b89f767503be6ab2b11e4f50a7557cb20066e667),
    immediately after the existing `oca-fix-manifest-website` hook entry. Do NOT add a new
    repo block, do NOT change the rev, do NOT add any `exclude` override on the new hook —
    it runs with always_run/pass_filenames=false and scans all addons. The global
    `^README\.rst$` exclude (anchored) does not match `addons/*/README.rst`, and the
    trailing-whitespace/end-of-file-fixer hooks already exclude `/README\.rst$`, so no
    additional exclude is needed (Pitfall 1).
    Then run `pre-commit run oca-gen-addon-readme --all-files` (or `pre-commit run --all-files`)
    to regenerate addons/l10n_py_base/README.rst and addons/l10n_py_account/README.rst from
    their readme/ fragment trees. The regeneration diff is purely cosmetic — commit it in its
    OWN dedicated commit: `chore(pre-commit): activate oca-gen-addon-readme baseline`, listing
    the config change + both regenerated README.rst files. Do not mix this diff with any other
    content change.
  </action>
  <verify>
    <automated>grep -c "oca-gen-addon-readme" .pre-commit-config.yaml && pre-commit run oca-gen-addon-readme --all-files</automated>
  </verify>
  <acceptance_criteria>
    - `.pre-commit-config.yaml` contains `oca-gen-addon-readme` exactly once, inside the maintainer-tools block (still pinned at rev b89f767503be6ab2b11e4f50a7557cb20066e667)
    - No new top-level `- repo:` entry was added (the maintainer-tools repo block count is unchanged)
    - `pre-commit run oca-gen-addon-readme --all-files` exits 0 (passes; after the baseline commit a re-run reports no file changes)
    - `addons/l10n_py_base/README.rst` and `addons/l10n_py_account/README.rst` both exist and are tracked
    - The README.rst regeneration is in a separate commit from the config change is acceptable, but at minimum the regeneration diff is NOT mixed with any docs/7x or meta-file content change
  </acceptance_criteria>
  <done>oca-gen-addon-readme active; both addon README.rst regenerated from fragments; cosmetic diff isolated in its own commit.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                                                 |
| ---------------------------- | ----------------------------------------------------------------------------------------------------------- |
| pre-commit hook → repo files | The oca-gen-addon-readme hook executes maintainer-tools code (pinned commit) that rewrites README.rst files |
| docs rename → linking files  | A rename with stale links produces broken navigation, not a security issue, but is integrity-relevant       |

## STRIDE Threat Register

| Threat ID | Category  | Component                                | Disposition | Mitigation Plan                                                                                                                                                                                                            |
| --------- | --------- | ---------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-03-01   | Tampering | oca-gen-addon-readme hook (supply chain) | mitigate    | Hook is added to an EXISTING maintainer-tools repo block already pinned at commit b89f767503be6ab2b11e4f50a7557cb20066e667; no new package install, no rev change, no new repo source. Pin is unchanged from Phase 1 D-02. |
| T-03-02   | Tampering | docs/60→65 rename link integrity         | mitigate    | grep gate in verify confirms zero remaining references to the old path in CLAUDE.md/AGENTS.md/PROJECT.md before merge                                                                                                      |
| T-03-SC   | Tampering | npm/pip/cargo installs                   | accept      | No new package-manager installs in this plan (RESEARCH §Package Legitimacy Audit: "no new packages"); the hook reuses an already-pinned OCA source                                                                         |

</threat_model>

<verification>
- `test -f docs/65_FASE_1_RETROSPECTIVA.md` (rename landed)
- `! grep -l "60_FASE_1_RETROSPECTIVA" CLAUDE.md AGENTS.md .planning/PROJECT.md` (links fixed)
- `grep -c "oca-gen-addon-readme" .pre-commit-config.yaml` returns 1
- `pre-commit run --all-files` exits clean (no pending regeneration)
- Regression guard: existing 97 Odoo tests unaffected (no addon source changed; only README.rst cosmetic regeneration)
</verification>

<success_criteria>

- docs/60 prefix freed; retrospectiva at docs/65 with all non-planning links updated
- oca-gen-addon-readme hook active in .pre-commit-config.yaml at the existing pin
- Both addon README.rst regenerated and committed in an isolated cosmetic commit
- `pre-commit run --all-files` clean
  </success_criteria>

<output>
Create `.planning/phases/03-bloque-c-documentaci-n-operacional/03-01-SUMMARY.md` when done
</output>
