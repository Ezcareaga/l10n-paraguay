---
phase: 03-bloque-c-documentaci-n-operacional
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - docs/70_ARCHITECTURE.md
  - docs/71_DEPLOYMENT.md
  - docs/72_RUNBOOK.md
autonomous: true
requirements: [DOC-05, DOC-06, DOC-07]
must_haves:
  truths:
    - "docs/70 shows C4 Context + Container (6 modules, shipped/planned markers), FE sequence diagram, and DTE state machine in Mermaid"
    - "docs/71 is a deploy blueprint (VPS, prod compose inline, Caddy, backup, health checks, update procedure) with Pre-Fase 3 markers, linking docs/60 for backup/network detail"
    - "docs/72 documents the 10 locked incidents with the fixed template + a single N1 to N3 escalation path"
  artifacts:
    - path: "docs/70_ARCHITECTURE.md"
      provides: "C4 + sequence + state machine architecture overview"
      contains: "C4Context"
    - path: "docs/71_DEPLOYMENT.md"
      provides: "Deployment blueprint with deferred-validation markers"
      contains: "Caddy"
    - path: "docs/72_RUNBOOK.md"
      provides: "10-incident operational runbook + escalation path"
      contains: "Escalation"
  key_links:
    - from: "docs/71_DEPLOYMENT.md"
      to: "docs/60_SECURITY_BASELINE.md"
      via: "cross-reference link (no content duplication)"
      pattern: "60_SECURITY_BASELINE"
    - from: "docs/72_RUNBOOK.md"
      to: "scripts/restore-smoke.sh"
      via: "incident 8 reference"
      pattern: "restore-smoke"
---

<objective>
Write the three operational docs in Spanish (D-01): docs/70_ARCHITECTURE.md
(C4 + sequence + DTE state machine in Mermaid, D-10/D-11), docs/71_DEPLOYMENT.md
(deploy blueprint with Pre-Fase 3 markers and inline prod compose, D-14), and
docs/72_RUNBOOK.md (10 locked incidents + escalation path, D-15).

Purpose: Cover DOC-05/06/07. These let an external dev understand the architecture,
how it deploys, and what to do for the 10 most likely incidents reading only docs/.
All three follow the blueprint style established in docs/60_SECURITY_BASELINE.md
(tables + bullets + code blocks + deferred-validation markers); docs/71 and 72
CROSS-REFERENCE docs/60 for backup/network rather than duplicating it.

Output: docs/70_ARCHITECTURE.md, docs/71_DEPLOYMENT.md, docs/72_RUNBOOK.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-bloque-c-documentaci-n-operacional/03-CONTEXT.md
@.planning/phases/03-bloque-c-documentaci-n-operacional/03-RESEARCH.md
@.planning/phases/03-bloque-c-documentaci-n-operacional/03-PATTERNS.md
@docs/60_SECURITY_BASELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/70_ARCHITECTURE.md (C4 + sequence + DTE state machine, Mermaid)</name>
  <files>docs/70_ARCHITECTURE.md</files>
  <read_first>
    - docs/60_SECURITY_BASELINE.md (header block + section structure analog — blueprint style, Spanish, tables + code blocks)
    - docs/50_MODULES_ROADMAP.md (the 6 modules with planned/shipped status for the C4 Container)
    - docs/03_DOMAIN_MODEL.md (source for the FE emission sequence + DTE lifecycle detail — docs/70 is overview, do NOT duplicate)
    - docs/01_SIFEN_KNOWLEDGE_BASE.md (DTE cycle, events, actors DNIT/SET)
    - .planning/phases/03-bloque-c-documentaci-n-operacional/03-RESEARCH.md (Pattern 5: Mermaid C4; Code Examples: DTE state machine; Pitfall 3: C4 rendering fallback)
    - .planning/phases/03-bloque-c-documentaci-n-operacional/03-PATTERNS.md (docs/70 section: header, C4 Context, state machine, shipped/planned marker patterns)
  </read_first>
  <action>
    Create docs/70_ARCHITECTURE.md (Spanish) using the docs/60 header block
    (`# Arquitectura — l10n-paraguay`, `**Estado:** diseño objetivo (Fase 2 EDI pending)`,
    `**Vigencia:**`, `**Cross-ref:**` to docs/50 + docs/03), then `## Alcance del documento`,
    then four Mermaid diagram sections:
    1. C4 Context (`C4Context`): actors Operador PyME, Odoo 18 + l10n_py_*, System_Ext SIFEN/DNIT,
       System_Ext SET/Marangatú, System_Ext Bancos (futuro), with Rel edges.
    2. C4 Container (`C4Container`): the 6 modules with EXPLICIT markers — shipped:
       l10n_py_base (18.0.1.1.0), l10n_py_account (18.0.1.0.0); planned: l10n_py_edi,
       l10n_py_reports, l10n_py_pos, l10n_py_withholding (label planned modules
       `[planned — Fase 2]` / `[planned]`), plus Odoo core + OCA deps (l10n_latam_base,
       l10n_latam_invoice_document).
    3. FE emission sequence (`sequenceDiagram`) labeled "diseño objetivo — Fase 2 EDI".
    4. DTE lifecycle (`stateDiagram-v2`) draft to posted to to_send to sent/error to cancelled,
       with a `note right of sent` saying "Diseño objetivo — Fase 2 EDI (l10n_py_edi no existe aún)".
    Use `C4Context`/`C4Container` keywords (Mermaid 11.x). Per Pitfall 3, sequenceDiagram and
    stateDiagram-v2 are stable; keep those types exactly. End with a `## Cross-references` block
    linking docs/50, docs/03, docs/71.
    Commit: `docs(architecture): C4 + sequence + state machine diagrams`.
  </action>
  <verify>
    <automated>test -f docs/70_ARCHITECTURE.md && grep -q "C4Context" docs/70_ARCHITECTURE.md && grep -q "C4Container" docs/70_ARCHITECTURE.md && grep -q "sequenceDiagram" docs/70_ARCHITECTURE.md && grep -q "stateDiagram-v2" docs/70_ARCHITECTURE.md && grep -q "l10n_py_edi" docs/70_ARCHITECTURE.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/70_ARCHITECTURE.md exists
    - Contains all four Mermaid block types: `C4Context`, `C4Container`, `sequenceDiagram`, `stateDiagram-v2`
    - C4 Container names all 6 modules: l10n_py_base, l10n_py_account, l10n_py_edi, l10n_py_reports, l10n_py_pos, l10n_py_withholding
    - Shipped modules carry their real versions (18.0.1.1.0 and 18.0.1.0.0); planned modules carry a `[planned` marker
    - The sequence diagram and state machine carry the "diseño objetivo — Fase 2 EDI" label
    - Header block matches the docs/60 style (Estado / Vigencia / Cross-ref); document is in Spanish
    - MANUAL: PR preview on github.com renders the C4 blocks (Pitfall 3 — fallback to `graph TD` only if C4 fails to render)
  </acceptance_criteria>
  <done>docs/70 contains the 4 required Mermaid diagrams with correct module set, versions, and Fase-2 design markers.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/71_DEPLOYMENT.md (deploy blueprint with Pre-Fase 3 markers)</name>
  <files>docs/71_DEPLOYMENT.md</files>
  <read_first>
    - docs/60_SECURITY_BASELINE.md (exact blueprint analog: header, per-eje Qué hacemos/Por qué/Comandos, the verbatim deferred-validation marker, Cross-references block at end; §Backup and §Red are the targets docs/71 LINKS to, not copies)
    - docs/61_COMPLIANCE_LEY_7593.md (referenced where deployment touches personal data)
    - infra/docker-compose.yml (the dev compose — prod compose in docs/71 is a derived inline reference, NOT this file)
    - .planning/phases/03-bloque-c-documentaci-n-operacional/03-RESEARCH.md (Pattern 3: blueprint doc; Security Domain: no real domain names, $VAR placeholders)
    - .planning/phases/03-bloque-c-documentaci-n-operacional/03-PATTERNS.md (docs/71 section: header, eje pattern, deferred marker, prod compose as inline code block, cross-ref block)
  </read_first>
  <action>
    Create docs/71_DEPLOYMENT.md (Spanish) using the docs/60 header (`# Deployment — l10n-paraguay`,
    `**Estado:** blueprint pre-deploy (Pre-Fase 3)`, `**Vigencia:**`, `**Cross-ref:**` to docs/60
    §Backup/§Red). Cover the DOC-06 axes, each as `## <eje>` with `### Qué hacemos` / `### Por qué`
    / `### Comandos ilustrativos`:
    - VPS y sistema operativo (specs, OS, ports)
    - docker-compose de producción — as an INLINE yaml code block labeled "código de referencia —
      no commiteado" (postgres:16 + odoo:18.0 with prod settings); do NOT create a docker-compose.prod.yml file
    - Caddy reverse proxy con SSL automático — inline Caddyfile snippet using a DOMAIN placeholder
      (e.g. tu-dominio.com.py), never a real domain
    - Backup Postgres automatizado — describe pg_dump + Backblaze B2 via rclone, but LINK to
      docs/60_SECURITY_BASELINE.md §Backup for the full strategy (no duplication; anti-pattern)
    - Health checks
    - Update procedure (module upgrade / -u flow)
    Every snippet block carries the verbatim marker `> Note: validar en Pre-Fase 3 cuando exista
    deploy real`. Use $VAR placeholders for any credential. End with `## Cross-references` linking
    docs/60, docs/61, docs/72, scripts/restore-smoke.sh.
    Commit: `docs(deployment): VPS blueprint with pre-fase-3 markers`.
  </action>
  <verify>
    <automated>test -f docs/71_DEPLOYMENT.md && grep -q "Caddy" docs/71_DEPLOYMENT.md && grep -q "60_SECURITY_BASELINE" docs/71_DEPLOYMENT.md && grep -q "validar en Pre-Fase 3" docs/71_DEPLOYMENT.md && grep -Eq "backup|pg_dump" docs/71_DEPLOYMENT.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/71_DEPLOYMENT.md exists
    - Contains sections covering: VPS, prod docker-compose (inline), Caddy, backup, health check, update procedure (grep `Caddy`, `pg_dump`/`backup`, `health` all match)
    - Contains at least one `> Note: validar en Pre-Fase 3 cuando exista deploy real` marker
    - Links docs/60_SECURITY_BASELINE.md (backup/network not duplicated)
    - No real domain name and no real credential string present (only placeholders / $VAR)
    - No docker-compose.prod.yml file was created (prod compose is inline only)
    - Document is in Spanish
  </acceptance_criteria>
  <done>docs/71 is a complete deploy blueprint with deferred markers, inline prod compose, and a backup cross-ref to docs/60 (no duplication).</done>
</task>

<task type="auto">
  <name>Task 3: Write docs/72_RUNBOOK.md (10 locked incidents + escalation path)</name>
  <files>docs/72_RUNBOOK.md</files>
  <read_first>
    - docs/60_SECURITY_BASELINE.md (blueprint style + §Backup for incident 8 cross-ref)
    - docs/01_SIFEN_KNOWLEDGE_BASE.md (DTE cycle, 72h legal plazo — source for incidents 7 and 10)
    - .planning/phases/03-bloque-c-documentaci-n-operacional/03-CONTEXT.md (D-15: the 10 locked incidents in exact order, fixed template, 3-level escalation, SIFEN-deferral markers on incidents 1/4/5/7/10)
    - .planning/phases/03-bloque-c-documentaci-n-operacional/03-RESEARCH.md (Pattern 4: incident template; Pitfall 4: do not invent SIFEN error codes — use generic curl/grep for SIFEN-dependent incidents)
    - .planning/phases/03-bloque-c-documentaci-n-operacional/03-PATTERNS.md (docs/72 section: header, incident template, escalation path table, backup cross-ref)
  </read_first>
  <action>
    Create docs/72_RUNBOOK.md (Spanish) with the docs/60 header (`# Runbook operacional —
    l10n-paraguay`, Estado/Vigencia/Cross-ref). Write exactly the 10 LOCKED incidents from D-15,
    in this order, each as `### Incidente N: <nombre>` using the fixed template
    (`**Síntoma:**` / `**Severidad:**` / `**Diagnóstico:**` with copy-paste bash / `**Resolución:**`
    numbered steps / `**Prevención:**`):
    1. SIFEN timeout · 2. Postgres disk full · 3. SSL cert expira · 4. CCFE expira ·
    5. Migración catálogos DNIT · 6. Timbrado vencido/agotado · 7. DTE rechazado por SIFEN ·
    8. Restore de backup falla en el test mensual · 9. Update de módulo rompe la DB (rollback) ·
    10. Cron/queue de envío EDI atascado (DTEs sin transmitir >72h).
    Incidents 1, 4, 5, 7, 10 (SIFEN-dependent) MUST carry the marker
    `> Note: procedimiento se valida en Fase 2 EDI / homologación.` and use generic curl/grep
    diagnóstico — do NOT invent specific SIFEN error codes (Pitfall 4). Incidents 6, 7, 10 may be
    documented concretely (ÑandeFact operational knowledge). Incident 8 cross-refs
    docs/60_SECURITY_BASELINE.md §Backup and scripts/restore-smoke.sh (do not reimplement the script).
    End with a single `## Escalation path` table: N1 Operador del deploy → N2 Careaga Dev
    (careagaezz@gmail.com, SLA orientativo) → N3 Externo (mesa ayuda SIFEN/DNIT, soporte VPS,
    comunidad OCA). Use $VAR placeholders for credentials.
    Commit: `docs(runbook): 10 incidents + escalation path`.
  </action>
  <verify>
    <automated>test -f docs/72_RUNBOOK.md && [ "$(grep -c '^### Incidente' docs/72_RUNBOOK.md)" -ge 10 ] && grep -q "Escalation" docs/72_RUNBOOK.md && grep -q "restore-smoke" docs/72_RUNBOOK.md && grep -q "Fase 2 EDI / homologación" docs/72_RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/72_RUNBOOK.md exists
    - `grep -c '^### Incidente' docs/72_RUNBOOK.md` returns ≥ 10 (DOC-07 gate)
    - The 10 incident headings match the locked D-15 list in order (SIFEN timeout, Postgres disk full, SSL cert expira, CCFE expira, Migración catálogos DNIT, Timbrado vencido/agotado, DTE rechazado, Restore falla, Update rompe DB, Cron/queue EDI atascado)
    - Incidents 1, 4, 5, 7, 10 contain the marker `procedimiento se valida en Fase 2 EDI / homologación`
    - Incident 8 references scripts/restore-smoke.sh and docs/60_SECURITY_BASELINE.md §Backup
    - A single `## Escalation path` table with N1/N2/N3 rows and careagaezz@gmail.com at N2
    - No invented SIFEN error code in any incident (SIFEN-dependent ones use generic curl/grep); no real credential strings
    - Document is in Spanish
  </acceptance_criteria>
  <done>docs/72 documents all 10 locked incidents with the fixed template, deferral markers on SIFEN incidents, and a single 3-level escalation path.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                                                      |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------- |
| operational docs → public repo | docs/71 + docs/72 contain deployment/runbook commands; leaking real secrets or infra detail is the relevant risk |
| docs/71 ↔ docs/60             | backup/network detail must be referenced, not duplicated (single source of truth)                                |

## STRIDE Threat Register

| Threat ID | Category               | Component                                 | Disposition | Mitigation Plan                                                                                                                                                                              |
| --------- | ---------------------- | ----------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-03-05   | Information Disclosure | docs/71 deployment snippets               | mitigate    | All credentials are $VAR placeholders; no real domain names (use tu-dominio.com.py); prod compose is illustrative inline, not committed. Acceptance criteria grep-gate forbids real secrets. |
| T-03-06   | Information Disclosure | docs/72 runbook diagnóstico commands      | mitigate    | Diagnostic commands use generic curl/grep + $VAR; SIFEN-dependent incidents carry the Fase-2 deferral marker rather than fabricated endpoint/credential detail (Pitfall 4)                   |
| T-03-07   | Tampering              | docs/71 duplicating docs/60 backup detail | mitigate    | docs/71 LINKS docs/60 §Backup/§Red; verify gate confirms the cross-ref link exists — prevents drift between two backup descriptions                                                          |
| T-03-SC   | Tampering              | npm/pip/cargo installs                    | accept      | No package installs in this plan (docs-only)                                                                                                                                                 |

</threat_model>

<verification>
- `grep -c '^### Incidente' docs/72_RUNBOOK.md` returns ≥ 10 (DOC-07)
- `grep -q "C4Context"` and `stateDiagram-v2` in docs/70 (DOC-05)
- `grep -q "Caddy"` and `60_SECURITY_BASELINE` in docs/71 (DOC-06, no-duplication cross-ref)
- No real credential or domain string in docs/71 or docs/72 (manual review of snippet blocks)
- Regression guard: 97 Odoo tests unaffected (docs-only)
</verification>

<success_criteria>

- docs/70 has C4 Context + Container + sequence + state machine (DOC-05)
- docs/71 covers all deploy axes as a blueprint with Pre-Fase 3 markers, no docs/60 duplication (DOC-06)
- docs/72 documents all 10 locked incidents + 3-level escalation path (DOC-07)
  </success_criteria>

<output>
Create `.planning/phases/03-bloque-c-documentaci-n-operacional/03-03-SUMMARY.md` when done
</output>
