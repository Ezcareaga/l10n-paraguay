---
phase: 03-bloque-c-documentaci-n-operacional
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - README.md
  - CHANGELOG.md
  - CHANGES.rst
autonomous: true
requirements: [DOC-01, DOC-02]
must_haves:
  truths:
    - "README.md reflects real module state (real versions, 97 tests, no TODO in shipped modules) and is OCA-style evaluator-first"
    - "CHANGELOG.md exists in Keep a Changelog 1.1.0 format with a [0.1.0] entry covering Fase 1 + Pre-Fase 2 foundation"
    - "CHANGES.rst is deleted (single repo-level changelog)"
  artifacts:
    - path: "README.md"
      provides: "OCA-style evaluator-first README in English"
      contains: "18.0.1.1.0"
    - path: "CHANGELOG.md"
      provides: "Keep a Changelog [0.1.0] entry"
      contains: "[0.1.0]"
  key_links:
    - from: "README.md"
      to: "CONTRIBUTING.md"
      via: "dev section delegating to CONTRIBUTING"
      pattern: "CONTRIBUTING.md"
    - from: "README.md"
      to: "docs/70_ARCHITECTURE.md"
      via: "documentation links table"
      pattern: "70_ARCHITECTURE"
---

<objective>
Rewrite README.md to OCA-style evaluator-first in English (D-01/D-02/D-03) and
create CHANGELOG.md in Keep a Changelog 1.1.0 format with the [0.1.0] entry
(D-06/D-08), deleting CHANGES.rst (D-06).

Purpose: Cover DOC-01 and DOC-02. The README is the file the OCA reviewer sees
first — it must show real module status (no "TODO" on shipped modules), real
versions, the docker-compose quick start, and links to docs/, delegating
contributor tooling to CONTRIBUTING.md. CHANGELOG.md replaces the CHANGES.rst
skeleton with a single repo-level changelog. Both are English (D-01).

This plan runs in Wave 3 because README links to docs/70 + docs/71 (created in
03-03) and to CONTRIBUTING.md (created in 03-05, same wave — link target filename
is fixed and known); ADRs (03-02) are referenced from the docs links table.

Output: README.md (rewritten), CHANGELOG.md (new), CHANGES.rst (deleted).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-bloque-c-documentaci-n-operacional/03-CONTEXT.md
@.planning/phases/03-bloque-c-documentaci-n-operacional/03-RESEARCH.md
@.planning/phases/03-bloque-c-documentaci-n-operacional/03-PATTERNS.md
@README.md
@CHANGES.rst
@infra/docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md (OCA-style evaluator-first, English)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state: badge header lines 3-8 to KEEP; "Módulos planificados" table with all-TODO to REPLACE; venv-first quick start lines 31-54 to REPLACE; codegraph/references blocks to MOVE to CONTRIBUTING)
    - infra/docker-compose.yml (the dev compose — quick start command is `docker compose -f infra/docker-compose.yml up -d`)
    - .planning/phases/03-bloque-c-documentaci-n-operacional/03-PATTERNS.md (README section: badge header KEEP, OCA addon table, quick start, links-to-docs patterns)
    - .planning/phases/03-bloque-c-documentaci-n-operacional/03-RESEARCH.md (D-02/D-03; State of the Art: current README factually wrong about module status)
    - references/l10n-brazil/README.md (OCA-style repo README analog — use bin/codegraph search if needed, do NOT read references/ manually)
  </read_first>
  <action>
    Rewrite README.md in English (D-01), OCA-style evaluator-first (D-02), in this order:
    1. KEEP the existing 6 badges verbatim (lines 3-8: CI, pre-commit, Security, codecov,
       License AGPL-3.0, Odoo) — do not add or remove badges unless one is broken.
    2. One-line "what it is" intro (Odoo 18 Community PY fiscal localization, DNIT/SIFEN).
    3. `## Available modules` table with REAL state — no "TODO" on shipped rows:
       l10n_py_base = 18.0.1.1.0 (23 tests), l10n_py_account = 18.0.1.0.0 (74 tests);
       l10n_py_edi / l10n_py_reports / l10n_py_pos / l10n_py_withholding = `planned`.
       Mention 97 tests total.
    4. `## Installation` (addons-path).
    5. `## Quick start` using the docker-compose flow from D-03: clone →
       `docker compose -f infra/docker-compose.yml up -d` → create DB
       (Name l10n_py_dev, Country Paraguay, Demo NO) → install l10n_py_base + l10n_py_account →
       login. End with a line delegating dev setup to CONTRIBUTING.md.
    6. `## Documentation` links table to docs/ by audience, including docs/00_OBJECTIVE.md,
       CONTRIBUTING.md, docs/71_DEPLOYMENT.md, docs/01_SIFEN_KNOWLEDGE_BASE.md,
       docs/70_ARCHITECTURE.md.
    7. Keep `## Security` (link SECURITY.md), `## License` (AGPL-3.0), `## Authorship` sections.
    MOVE the codegraph/references/venv tooling blocks OUT of README (they go to CONTRIBUTING.md
    in plan 03-05 — do not duplicate them here). DELETE the stale `## Estado` paragraph that
    says "Aún no hay código de módulos publicado" (factually wrong — 2 modules shipped).
    Commit: `docs(readme): rewrite evaluator-first per D-02`.
  </action>
  <verify>
    <automated>test -f README.md && [ "$(grep -c 'TODO' README.md)" -eq 0 ] && grep -q "18.0.1.1.0" README.md && grep -q "18.0.1.0.0" README.md && grep -q "docker compose -f infra/docker-compose.yml" README.md && grep -q "CONTRIBUTING.md" README.md && grep -q "70_ARCHITECTURE" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TODO" README.md` returns 0 (DOC-01 gate)
    - README contains both `18.0.1.1.0` and `18.0.1.0.0` (real shipped versions) and mentions 97 tests
    - The 6 existing badges are still present (grep `codecov` and `AGPL-3.0` both match)
    - Quick start contains `docker compose -f infra/docker-compose.yml up -d`
    - Links to CONTRIBUTING.md, docs/70_ARCHITECTURE.md, docs/71_DEPLOYMENT.md present
    - No codegraph/venv/references tooling block remains in README (moved to CONTRIBUTING)
    - The stale "Aún no hay código de módulos publicado" line is gone
    - README is in English (D-01)
  </acceptance_criteria>
  <done>README is OCA-style evaluator-first in English, shows real module status with no TODO, and delegates dev tooling to CONTRIBUTING.md.</done>
</task>

<task type="auto">
  <name>Task 2: Create CHANGELOG.md ([0.1.0] Keep a Changelog) and delete CHANGES.rst</name>
  <files>CHANGELOG.md, CHANGES.rst</files>
  <read_first>
    - CHANGES.rst (current skeleton content to migrate then delete)
    - docs/65_FASE_1_RETROSPECTIVA.md (source material for [0.1.0] Added/Changed entries — note: renamed from docs/60 in plan 03-01)
    - docs/60_SECURITY_BASELINE.md (Pre-Fase 2 Bloque B work to list in [0.1.0])
    - .planning/phases/03-bloque-c-documentaci-n-operacional/03-RESEARCH.md (Code Examples: Keep a Changelog [0.1.0] entry structure; Pitfall 6: include Bloque A CI + Bloque B security work)
    - .planning/phases/03-bloque-c-documentaci-n-operacional/03-PATTERNS.md (CHANGELOG.md section: full Keep a Changelog structure)
  </read_first>
  <action>
    Create CHANGELOG.md in English (D-01), Keep a Changelog 1.1.0 format. Structure:
    `# Changelog`, the standard intro line linking keepachangelog.com/en/1.1.0/,
    an `## [Unreleased]` section, then `## [0.1.0] - Unreleased (see Phase 4 REL-05 for tag date)`
    (D-08 — date completed when Phase 4 tags; do not invent a date).
    `### Added` MUST list (Pitfall 6 — include Bloque A + B, not just modules):
    - l10n_py_base 18.0.1.1.0 (DNIT/SIFEN catalogs, l10n_py.timbrado, res.company PY extension,
      RUC/CI validation módulo 11, 23 tests)
    - l10n_py_account 18.0.1.0.0 (chart of accounts, IVA taxes 10%/5%/exenta,
      l10n_latam.document.type FE/NC/ND/NR, account.journal timbrado extension, 74 tests)
    - CI/CD pipeline (lint + test OCA py3.10/PG12 matrix + commitlint + dependabot)
    - Security workflow (gitleaks + Bandit + Dependency Review)
    - SECURITY.md, docs/60_SECURITY_BASELINE.md, docs/61_COMPLIANCE_LEY_7593.md
    `### Changed`: branch protection on main (6 status checks), README restructured OCA-style.
    Add the `[0.1.0]` link reference at the bottom pointing to the v0.1.0 release tag URL.
    Then DELETE CHANGES.rst via `git rm CHANGES.rst` (single repo-level changelog, D-06).
    Per-addon history stays in addons/*/readme/HISTORY.rst per OCA — do not touch those here.
    Commit: `docs(changelog): add [0.1.0] entry, replace CHANGES.rst`.
  </action>
  <verify>
    <automated>test -f CHANGELOG.md && ! test -f CHANGES.rst && grep -q "\[0.1.0\]" CHANGELOG.md && grep -q "Keep a Changelog" CHANGELOG.md && grep -q "l10n_py_base 18.0.1.1.0" CHANGELOG.md && grep -Eq "gitleaks|Bandit" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md exists; CHANGES.rst does not (git rm)
    - CHANGELOG contains `[0.1.0]`, `Keep a Changelog`, and the keepachangelog.com/en/1.1.0/ link
    - `### Added` lists both modules with real versions AND the CI/CD pipeline AND the security workflow (Pitfall 6 — `gitleaks` or `Bandit` present)
    - `### Changed` mentions branch protection and README restructure
    - The [0.1.0] heading marks the date as unreleased / deferred to Phase 4 (no fabricated date)
    - CHANGELOG is in English (D-01)
  </acceptance_criteria>
  <done>CHANGELOG.md replaces CHANGES.rst with a complete [0.1.0] entry covering Fase 1 + Bloque A + Bloque B work.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                                           |
| ------------------------------ | ----------------------------------------------------------------------------------------------------- |
| README/CHANGELOG → public repo | These are the most-read public files; only risk is accidentally exposing infra detail or wrong status |

## STRIDE Threat Register

| Threat ID | Category               | Component                        | Disposition | Mitigation Plan                                                                                                                                                   |
| --------- | ---------------------- | -------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-03-08   | Information Disclosure | README quick start / CHANGELOG   | mitigate    | Quick start uses default dev creds (admin/admin) explicitly flagged "change on first login"; no real secrets. CHANGELOG lists feature names only, no credentials. |
| T-03-09   | Repudiation            | CHANGELOG [0.1.0] date integrity | mitigate    | Date stays "Unreleased (see Phase 4 REL-05)" — not falsely dated before the v0.1.0 tag exists                                                                     |
| T-03-SC   | Tampering              | npm/pip/cargo installs           | accept      | No package installs in this plan (docs-only)                                                                                                                      |

</threat_model>

<verification>
- `grep -c "TODO" README.md` returns 0 (DOC-01)
- `grep "\[0.1.0\]" CHANGELOG.md` matches (DOC-02)
- `! test -f CHANGES.rst` (migration complete, D-06)
- `pre-commit run --all-files` clean (prettier may reformat the new .md — expected, re-stage)
</verification>

<success_criteria>

- README is OCA-style evaluator-first, English, real module status, no TODO (DOC-01)
- CHANGELOG.md [0.1.0] in Keep a Changelog format covering Fase 1 + foundation (DOC-02)
- CHANGES.rst removed
  </success_criteria>

<output>
Create `.planning/phases/03-bloque-c-documentaci-n-operacional/03-04-SUMMARY.md` when done
</output>
