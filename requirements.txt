# Python deps de los addons l10n_py_* que no están en el mapping de
# setuptools-odoo ni en los requirements de Odoo core.
# La imagen OCA CI (oca_install_addons) instala este archivo automáticamente.
signxml>=4.0
zeep>=4.2
requests-pkcs12>=1.25
# lxml>=5.2 separó lxml.html.clean en package propio; Odoo lo importa.
# Necesario porque signxml>=4.0 fuerza lxml>=5.2.1 en la imagen OCA CI.
lxml_html_clean
# signxml>=4 y services/certificate.py (not_valid_*_utc) requieren
# cryptography>=42; pyOpenSSL 21.0.0 (pin de la imagen CI) es incompatible
# con esa ABI (X509_V_FLAG_NOTIFY_POLICY removido) — subir en tándem.
cryptography>=42
pyopenssl>=26.3.0
# urllib3 1.26.5 (pin de la imagen CI) usa un API privado de cryptography
# removido en >=42, y toda la serie 1.x tiene CVEs high que bloquean
# dependency-review. Odoo 18 funciona con 2.x (validado en container dev).
urllib3>=2.0
